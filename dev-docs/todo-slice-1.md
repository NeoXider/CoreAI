# TODO slice audit — unchecked items, TODO.md lines 60–397

Method: grepped each named symbol, opened the file, checked behaviour. Cross-checked `Assets/CoreAI/CHANGELOG.md` and `Assets/CoreAiUnity/CHANGELOG.md` for later closures — no closure entry found for any item below (only background mentions). A prior `dev-docs/TODO_AUDIT_2026-09-04.md` exists but was used only as a lead, not as evidence; every bucket below rests on a file:line I opened.

| TODO.md line | Item summary | Bucket | Evidence file:line | Next step |
|---|---|---|---|---|
| 60 | Host restore envelope: package restore writes run outside a host envelope | OPEN | `Assets/CoreAIMods/Runtime/Infrastructure/RbxWorldPackageSerializer.cs:274-307` (`RestoreFresh` takes no envelope; passes `worldAclVersion: null` at :306); caller `Assets/CoreAIMods/Runtime/Infrastructure/RbxWorldPackageContracts.cs:568` stages restore with binder/sink/rig only, no envelope | Add host scope around restore writes + RED test restoring an ACL-versioned package through production composition; touch the two files above + `RbxWorldHost.cs` |
| 77 | Demo scene orphans: `ChatPromptButtonsController` in 3 scenes; binder no-op in WaveAutoBattler | OPEN | Orphans still in scenes: `Assets/CoreAI.Demos/LiveMechanicsMods/LiveMechanicsModsChatDemo.unity:150`, `Assets/CoreAI.Demos/MiniRpg/MiniRpgModsDemo.unity:1472`, `Assets/CoreAI.Demos/ModdableUnits/ModdableUnitsDemo.unity:872`; `WaveAutoBattlerModsDemo.unity` grep shows no `CoreAiChatPanel` object, so `Assets/CoreAI.Demos/Shared/Scripts/DemoHubPagesBinder.cs:61-66` `FindFirstObjectByType<CoreAiChatPanel>` finds null → no-op | Open the 3 scenes in Editor: enable chat example menu or delete controller; give WaveAutoBattler a chat panel or scope the binder call; save scenes |
| 80 | IMGUI migration backlog: 13 files on ratchet allowlist | OPEN | `Assets/CoreAIMods/Tests/EditMode/ImguiBanRatchetEditModeTests.cs:54-72` — all 13 entries still present (10 demo + 3 overlays) | Migrate one file to UITK/uGUI at a time, delete its entry; ratchet enforces shrink |
| 82 | Incremental JSON/ZIP encode-decode on WebGL (4 MiB / 4096 / bounded collections) | OPEN | Fail-fast budget still the implementation: `Assets/CoreAIMods/Runtime/Infrastructure/FileRbxWorldPackageStore.cs:209-212` constants + refuse messages at `:556-561`, `:580-585`, `:601-606`; no chunked/streaming codec found (grep `Incremental\|chunked` in Infrastructure: only the refuse strings) | Design incrementally-chunked codec or raise/document budget; touch world-package serializer + store |
| 84 | Hostile-reader quotas + preorder/sorted canonicality | OPEN | No token-materialization quota or hostile-reader canonicality enforcement found (grep `hostile-reader\|canonicality\|token.*quota` in Assets: only TODO.md + QA report). Partial base exists — deterministic write walk `Assets/CoreAIMods/Runtime/RbxApi/Instances/InstanceTreeSerializer.cs:10` — but the reader-side enforcement described is absent | Add token quota before semantic validation + preorder/sorted-collection canonicality check in package reader |
| 92 | Inherited `ConfigureAwait(false)` sites frozen in WebGL guard allowlist | OPEN | Allowlist still populated: `Assets/CoreAiUnity/Tests/EditMode/WebGlUnsafeAsyncPrimitivesEditModeTests.cs:142-168` (memory-contour + 5 tool bodies + decorators/orchestrator entries, each labelled "унаследовано … отдельной задачей") | Convert each to host-context awaits (+ `MeaiToolTaskBridge.Publish` for tool bodies per `Assets/CoreAIMods/Runtime/LuaExecution/LuaTool.cs:85-91` pattern), drop its entry |
| 97 | Browser-gate harness: keep pane painted (visibility / frame-rate check) | OPEN | No visibility/frame-rate check in G11 path found (grep `requestAnimationFrame\|G11.*visib\|frame-rate check\|visibility.*G11` in Assets: zero hits); G11 record confirms the stall premise (`dev-docs/G11_RUN_RECORD_2026-09-02.md` attempt-5 trace) but the protocol guard was not added | Add visibility/frame-rate pre-check to the G11 protocol before any timed row |
| 101 | Weak tests: symmetric `RbxSpace` oracle, once-per-load deprecation, per-shape `At1To1` | OPEN | `Assets/CoreAIMods/Tests/EditMode/RbxApi/Acceptance/Mvp1ConversionLintEditModeTests.cs:145-146` expected computed via `RbxSpace.ToUnityPose/SizeToUnity`; `Assets/CoreAIMods/Tests/EditMode/RbxApi/LuaBindings/RbxApiLuaBindingsEditModeTests.cs:1576-1597` two `Instance.new` calls in one `LoadMod`, asserts count==1 (a once-per-load impl also passes); `AssetRule_At1To1_OnlyTheConstantChanges` at `Assets/CoreAIMods/Tests/EditMode/RbxApi/Binding/InstanceGameObjectBinderEditModeTests.cs:305` is Block-scoped | Rewrite each with independent oracle / reload twin / all-shapes coverage |
| 107 | Final QA verdict umbrella (G10 admission, heap slope, host envelope) | OPEN | Umbrella over rows above, all still open: admission `MaxPending=64` static at `Assets/CoreAI/Runtime/Core/Features/Orchestration/AiOrchestrationQueueOptions.cs:18`; heap fail-fast/scale rows per slice; envelope row per :60 | Closes only when the G10 admission, heap-slope, and envelope rows close; no separate action |
| 147 | Top-level `task.wait` as its own design rung | DEFERRED | Deliberately postponed by the item itself; code now runs chunks as scheduler threads (`Assets/CoreAIMods/Runtime/Scripting/LuaCs/LuaCsRbxApiBindings.cs:535-552` `RunModChunk`) and `Assets/CoreAIMods/Tests/EditMode/RbxApi/LuaBindings/RbxApiLuaBindingsEditModeTests.cs:2617-2639` pins top-level `task.wait` suspend/resume — but the item demands a design pass over sync-load-error semantics (`IsLoaded`, quarantine, `LoadFails` assertions), which has not happened | Unblocked by a design pass with acceptance criteria for scheduler-owned mod entry; do not bolt onto a release |
| 155 | Deferred drain points revisit when signals arrive (R5.4–R5.7) | UNVERIFIABLE | Searched `ModScheduler.cs` drain/drain-buffer code (`:310-332` stage table, `:780-838` drains) — interleaved `DrainDeferred`/`DrainSignals` exist, but I did not open the R4.8 vs R5.4–R5.7 spec text to say which drain point is missing | Inconclusive: compare the R5.4–R5.7 spec against `ModScheduler.cs:300-340` stage table and name the missing drain |
| 212 | PlayMode gate: live part blocked on local LLM backend | UNVERIFIABLE | Item is an environment-gated test run (needs LM Studio model + `COREAI_TEST_MODEL`), not checkable by code reading; tried grepping the four named live fixtures — names not found verbatim in Tests tree, so I could not even confirm their current names | Inconclusive: load a model, point `COREAI_TEST_MODEL` at it, re-run PlayMode; then mark DONE or file failures |
| 245 | Play-test both samples in Play Mode | UNVERIFIABLE | No playtest record found in `dev-docs/` beyond arithmetic hand-verification claims; playing samples requires the Editor/player, which this task forbids | Inconclusive: play both samples in Play Mode after the camera/collision rewrites; record result |
| 372 | Comment-convention cleanup wave + `strip_non_doc_comments.py` caveat | OPEN | Script still strips `// WHY:`/`// HACK:`: `tools/strip_non_doc_comments.py:268-275` keeps `///` and TODO only, drops every other `//` line via `strip_line_comment_and_blocks` | Fix/replace the strip script to preserve `WHY`/`HACK`, extend beyond 2 packages, then run the wave per package |
| 379 | `SemaphoreSlim.Wait()` reentrancy on WebGL (5 stores + `ISkillStore`) | OPEN | Sync gates still present, no reentrancy guard found: `Assets/CoreAI/Runtime/Core/Features/AgentMemory/FileConversationSummaryStore.cs:80,141,196` `_gate.Wait()`; `Assets/CoreAI/Runtime/Core/Features/Llm/ISkillStore.cs:127` `gate.Wait()`; grep `reentrancy\|Reentrancy\|recursion guard` in Runtime: zero hits | Decide guard vs document; touch the 5 file stores + `ISkillStore` |
| 383 | WebGL canary for Lua↔C# sync-over-async bridge | OPEN | Reliance sites exist (`Assets/CoreAIMods/Runtime/Scripting/LuaCs/LuaCsExecutionGuard.cs`, `LuaCsSecureEnvironment.cs`, `LuaCsCoroutineHandle.cs`); grep `canary\|Canary` in Assets/Tests: zero hits — no canary test exists | Add a WebGL-build canary test failing CI if suspended resumes stop completing synchronously |
| 387 | WebGL persistence decision for summaries + token calibration | DEFERRED | Split state confirmed: desktop `FileConversationSummaryStore` vs WebGL in-memory per `Assets/CoreAiUnity/Docs/ARCHITECTURE.md:108`; `CoreAILifetimeScope` WebGL skip per changelog `Assets/CoreAiUnity/CHANGELOG.md:3436`. The item asks for a won't-fix-vs-sink decision, which is a judgment call, not code | Unblocked by deciding won't-fix (document) vs injectable flush sink; then implement or close |
| 391 | Editor tooling zero test coverage | UNVERIFIABLE | Searched `*Editor*Tests*.cs` for scene-creator / `CoreAIModuleManager` / importer coverage — too broad a surface to exhaust with remaining budget; no conclusion drawn | Inconclusive: grep the 20 editor classes for test references, list uncovered user-facing paths |
| 394 | Untested MCP tools + Hub UI | OPEN | Tools live but testless: `Assets/CoreAIMcp/Runtime/Tools/GetModLogsMcpTool.cs:16`, `ManageModsMcpTool.cs:17` registered in `CoreAiMcpToolProvider.cs:57-67`; grep `GetModLogsMcpTool.*Test\|ManageMods.*EditModeTests\|WorldCommandMcpTool.*Test\|MainCameraScreenshotSource.*Test\|HubAboutPage.*Test` repo-wide: zero hits | Add EditMode coverage for the 4 tools/source + `HubAboutPage`/`HubBuiltInPages`/`HubPageWidgets` |
| 397 | Vision wire-format unverified live | OPEN | Code marks itself unverified: `Assets/CoreAiUnity/Runtime/Source/Features/Vision/VisionSelfProbe.cs:100-102` TODO ("verify the image-content wire shape live"); round-trip caller at `Assets/CoreAIHub/Runtime/HubSettingsPage.cs:1380-1388` exercises health, not the image shape | Run one live probe per supported backend; keep the TODO until then |
