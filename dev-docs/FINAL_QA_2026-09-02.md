# Final QA — 2026-09-02 (branch feature/mvp2-multiplayer, HEAD 5c62c43d / e3320bb0)

Judge: independent adversarial, read-only on code. Evidence files: `artifacts/testresults/g9.xml` (EditMode 3256/3247/0/9, 2026-09-02 17:56), `pm4.xml` (PlayMode 114/110/0/4), `dev-docs/G11_RUN_RECORD_2026-09-02.md`, `artifacts/scale/scale-baseline.json` + `scale-baseline.md`, `dev-docs/SCALE_CHARACTERIZATION.md`, `PROGRESS.rungzero.md`, `PROGRESS.wffin.md`, `PROGRESS.texqa.md`.

Production-path rule §3b: a helper-only/fixture-only proof does not count. Negative twin must be executed. A test that cannot fail (cannot be RED) is vacuous.

---

## 1. MVP2_ACCEPTANCE_MANIFEST G1–G12

| Gate | Verdict | Deciding file:line / record |
|---|---|---|
| **G1 auth mods** A→B unload/reload/revert/forget refused | **PASS** | `RungZeroAclEditModeTests.SetAccessControl_WithOnlyRegistryReference_CannotChangeAnotherActorsInstance` + `DestroyInstance_*` PASS in g9.xml; `LuaCsModRuntime.cs:766-786` owner check names actor+reason. Negative twin executed. Production path: engine-free registry authorizer (`InstanceRegistry.cs` via `WorldAclAuthorizer` in `CoreAI.RbxApi.Instances`) — not Lua-only. |
| **G2 auth world** ACL-versioned cross-actor mutate/destroy refused | **PASS (versioned only)** | `Lua_WorldAcl_OwnerCanMutate_ButCrossActorWritesAndDestroyAreDenied`, `LegacyWorldKeepsExistingCrossActorDestroyBehavior`, `ReparentChecks*`, `RecursiveDestroy*PreflightEveryDescendantAtomically` PASS. Manifest G2 scope explicitly excludes legacy `WorldAclVersion==null` (`INSTANCE_REGISTRY.cs:138-141`) — legacy not covered by security claim. Negative twin executed. |
| **G3 host-protected** writable Camera, not destroyable/reparentable | **PASS** | `Lua_WorldAcl_HostProtectedCameraIsWritable_ButSingletonLifecycleIsHostOnly` PASS; `LuaCsRbxInstanceBindings.cs:387-401` early-return for `IsUnrestricted` only checks destroy/reparent. Negative twin executed. |
| **G4 chat privacy** A→B history/rate not observable | **PASS** | `ActorKeyedInGameLlmChatServiceFactory.cs:49-79` per-actor service bounded 256; tests `QueuedAiOrchestrator` 0 cross-actor cancellations in manifest §5.1/§5.2. Negative twin: existing G4 suite asserts B history invisible (g9 PASS set includes chat isolation via factory). |
| **G5 quotas** N succeeds, N+1 refused naming actor+reason; absent quotas fails | **PASS** | `LuaCs_RegisteredInstanceQuota_IsPerActorAtNAndNPlusOne`, `SchedulerThreadQuota_*`, `EventSubscriptionQuota_*` PASS in g9.xml. Each names actor+limit+group. Negative twin at N+1 executed. Absent-quota failure gated by existence of quota tests — would FAIL if quotas removed. |
| **G6 event routing** 1 subscriber invoked, 19 not | **PASS through production** | `LuaCsEventRoutingProductionPathEditModeTests.RegisterCoreAiMods_OneSubscriberLeavesNineteenNonSubscriberQueuesUntouched` (production composition `RegisterCoreAiMods`) PASS; `LuaCs_EmitEvent_RoutesOnlyToSubscribers_AndTouchesOnlySubscriberEntries` asserts touched-entry count == subscribers. 0 non-subscriber invocations across 22,800 checks recorded in manifest §5.1. Negative twin executed. |
| **G7 no global fan-out lock** emit holds no process-wide lock | **PASS** | `LuaCs_EmitEvent_HasNoSynchronizationBeforePerSubscriberEnqueue` + `PerSubscriberQueueLockIsTheAllowedSynchronizationBoundary` PASS; `LuaCsModRuntime.cs:2536-2557` `Volatile.Read(snapshot)` confirmed. Structural test — not vacuous (would RED if lock reintroduced). |
| **G8 reconnect** same durable actor resumes, duplicate op idempotent, no fork | **PASS** | `Reconnect_RotatesSessionWithoutChangingActorId`, `LuaCs_MutationEnvelope_ReconnectingDurableActor_DoesNotDoubleApplyOrForkState`, `DuplicateOperationId_ReturnsFirstProductionResult`, `ConcurrentSameRevision_DeterministicallyOrdersFirstEntrant` PASS in g9.xml. Negative twin: duplicate returns first result, stale revision refused. |
| **G9 engine-free ports** asmdef no Unity refs transitively | **PASS** | `RungZeroAclEngineFreeTests.WorldAclAuthorizer_LivesInEngineFreeAssembly` + `RungZeroAclEditModeTests.WorldAclAuthorizer_LivesInEngineFreeInstancesAssembly` PASS; `NetworkBridgeEditModeTests` + fitness tests `RbxApiInstancesArchitectureFitnessEditModeTests.cs:28,87-110` (INetworkBridge in `CoreAI.RbxApi.Instances`, `noEngineReferences:true`, no `UnityEngine` refs). |
| **G10 chat throughput** ≥95% served, p95 ≤5s, 0 cross-cancel, no starvation | **FAIL** | Manifest §5.2 REAL-PROVIDER: staggered 10/40 (0.25) p95 72.0s, burst 23/40 (0.575) p95 52.4s — both fail served fraction and p95. Scale staircase (stub 100ms) at 100: 96 refused, p95 1.79s but admission fail; at 200: 396 refused — full gate overallPass 0 at 4ms and 16ms (`scale-baseline.json` steps[2..3] `chatGatePass:false`). Zero cross-cancel held (0) but throughput gate failed. |
| **G11 WebGL** §6.5 checklist in real browser | **NOT PROVEN (partial PASS)** | Boot, Lua `SELFTEST_DONE checks=16 fails=0` + Tetris, real-provider nonce `NONCE-7d3f`, persistence `Save Now→Yes→reload→Yes / Reset→No→reload→No`, materials `MATERIAL_CATALOG complete slots=46 mapped=45 fallback=1 result=PASS` (36 textured), 15 demos 0 console errors — all PASS in `G11_RUN_RECORD.md` on player `5c62c43d` 103 MB at `127.0.0.1:8777` Chrome/148. **Retry/terminal/recovery row PENDING** (record), MCP outbound HTTP NOT RUN, template crop at 768px noted. TODO claims rebuild `c38fe6fe` retried 503 → nonce, then terminal error 46s, recovery without reload — but no updated record/log attached. Honest mark = pending, so G11 not yet satisfied per manifest “static checklist alone does not satisfy”. |
| **G12 corpus** frozen 20 ids, ≥30% unmodified, catalog 2026-09-01 17/20=85% | **PASS** | g9.xml TierA_TAC-001..020 all 20 PASS; `FrozenCatalog_HasTwentyUniqueFixtures*` PASS. Manifest requires ≥30% (85% previously), now 100% in this run. Unmodified vs modified classification retained in manifest, not re-measured here — but ≥30% threshold trivially met. |

## 2. MVP25_ONLINE_PLAN §1 entry gates P1–P5

| Gate | Verdict | Evidence |
|---|---|---|
| **P1 full EditMode** all expected discovered, 0 fail/skip, full suite not filter | **NOT PROVEN** | g9.xml 3256/3247/0/9 — 0 failed, but 9 skipped (6 `NeoxiderPagesChangedBehaviorTests` Ignored + 3 `RbxApi4BLiveCheck` Explicit). Manifest demands 0 skipped/ignored for `g9`. Skips are expected explicit/live-check, but frozen expected count not published in this branch, so “discovered==expected” unverifiable. Batch was full-suite (not filtered) — pass on that half. |
| **P2 independent QA** every round complete, findings closed, final round after last fix | **FAIL** | Rounds recorded: `ARCH_AUDIT_ONLINE_2026-09-02.md`, `DOCS_AUDIT_2026-09-02.md`, `PROGRESS.qa-world.md`, `PROGRESS.texqa.md`, etc. Final fixes: rung-zero landed 2026-09-02 (`PROGRESS.rungzero.md`) and wffin follow-ups — no QA round ran *after* `5c62c43d` containing those fixes (G11 record predates wffin hub change). So final round predates last fix. |
| **P3 MVP1 re-check** 4 PARTIAL + 1 FALSELY CLAIMED → full PASS with adverse cases | **PARTIAL / NOT PROVEN through production** | Former partials (qa-mvp1b.md 2026-08-31): 9 Color3 rounding, 11 Wedge/CornerWedge, 13 loud-stub context, 14 deprecation once-per-mod, 12 lazy host wrap FALSELY CLAIMED. Now: `Color3_FromRGB_FractionalChannelsUseLockedCoreAiRounding` PASS, `CornerWedge_MaterializesCustomTwoSlopeMesh` PASS, lazy `Lookup_ByWorldName_LazilyWrapsHostObject_AndEveryKeyResolvesTheSameRecord` PASS, loud stubs phased. Adverse cases present (e.g. wrong-type BAD_ARGUMENT). **But** `Mvp1AcceptanceHarness.cs:35-51` hand-builds binder/registry/game/bindings — §3b production-path proof still bypassed for the golden-tree gate (noted in qa-mvp1b Log). So 4/5 are code-fixed and test-passing; the harness composition defect remains open. |
| **P4 MVP2 manifest** frozen workload, non-zero counters, production-path, twins | **FAIL** | Fails because G10 fails and G11 incomplete; workload frozen SHA `6f5d18…` + `c5726c…` + `305f80…` present, non-zero counters enforced (`steps 1886@20 … 18866@200`, packets/bytes >0, `zeroCounters:[]` except 100/200 chatGate fail), negative twins present for G1-G9. Prose-only substitute not used. Still a hard FAIL via G10. |
| **P5 mutation envelope** actor/target/opId/expectedRev, duplicate idempotent, stale refused, reconnect | **FAIL (one gap)** | Envelopes: `RungZeroEnvelopeProductionPathsEditModeTests` 6/6 PASS (LoadMod chunk, scheduler resume, heartbeat handler, RemoteEvent handler, cross-mod callee, bare destroy refused) + `PlainExecuteLua_ServerEnvelope_AppliesOneMutationBatch`, `MCP Invoke_WithIdentity_UsesServerGeneratedEnvelopeOverload`, `MutationOutsideEnvelope_InAclWorld_IsRefused`, `ExplicitDuplicateOperationId_AppliesOnceThroughRealExecutor` PASS. **Gap:** `TODO.md` “Rung zero residue — host restore envelope” OPEN: `RbxWorldPackageSerializer.cs:274`, `RbxWorldPackageContracts.cs:568`, `RbxWorldHost.cs` writes do not run under host envelope — out of rung-zero file scope. So P5 not closed. |

## 3. MVP25 §4.1 W3.1–W3.4 (world package)

| Gate | Verdict | Evidence |
|---|---|---|
| **W3.1 restart round-trip** golden tree, stable ids, props/attrs/settings/owners/origins/mod sources | **PASS (batch)** | `WorldOwnedPayload_WithPackagedLuaSources_CodecRestoreRecapture_RoundTrips` PASS in g9.xml; `Docs/CoreAIMods/WORLD_PACKAGE.md` one serializer for disk + `ExportSnapshot`, deterministic manifest/world/mods. |
| **W3.2 one serializer** disk load ≡ snapshot payload for every field | **PASS** | Same codec path; no second mapper found. Statement “one canonical serializer for disk and ExportSnapshot” in WORLD_PACKAGE.md matches code. |
| **W3.3 clean mod restart** exactly once after restore, no ephemeral survivor | **PASS** | `RbxWorldRuntimeSessionController` staged replacement (zero-await publication, old hooks/connections/scheduler killed) — `PROGRESS.rungzero.md` + WORLD_PACKAGE §Production session replacement. Tests for dormancy/correlation pass. |
| **W3.4 backup safety** manual slot byte-stable, pre-mutation autosave, ring rotates | **PARTIAL / NOT PROVEN in batch** | Code present: `FileRbxWorldPackageStore` two-phase durability, just-confirmed never evicted, `ConfirmedWorldMutationGate` single-flight capturing before mutation. Off-device 6 tests `Mvp3WorldPackageFollowUpEditModeTests.cs` (diagnostics, ListAutoSaves, pre-load `load_world-pre`, reserved names) 6/6 GREEN (`PROGRESS.wffin.md`). **But** those 6 not discovered in g9.xml (compile Include recently added, predated by `CoreAI.Mods.Tests.csproj` stale include / `InstanceSnapshot` mismatch noted in `PROGRESS.texqa.md:20`). So batch evidence missing; manual↔autosave isolation and rotation atomicity not in g9. |
| **W3.1 addendum — dangling PrimaryPart diagnostic** | **IMPLEMENTED, batch not discovered** | Snapshot clears `PrimaryPart` to null, records versioned `diagnostics` entry (`model_id`, `dropped_primary_part_id`, `reason=mod-ephemeral|missing`), live tree untouched; injected mod-owned node still rejected. Code in `InstanceTreeSerializer.cs:194` / WORLD_PACKAGE.md “Retained World…” para. Test `Capture_DurableModelReferencingModEphemeralPrimaryPart_DropsReferenceAndEmitsDiagnostic` renamed from failure — GREEN off-device, absent from g9.xml. |
| **W3.4 addendum — autosave listing/loading + pre-load safety** | **IMPLEMENTED, Hub UI partial** | `list_autosaves`/`load_autosave` through same confirm/reject pool (`RbxWorldPackageContracts.cs:444-452` + CHANGELOG), `load_world-pre` safety autosave with confirmed durability before swap, Hub World Loads lists autosaves and routes Load… through confirm pool. `PROGRESS.wffin.md` fixes docs; `PROGRESS.hubauto.md` shows Hub autosave UI still *in-progress* (checklist open). Pending batch UI tests. |

## 4. Texture/materials (TEXTURE_MATERIALS.md + WORLD_PACKAGE)

| Claim | Verdict | Evidence |
|---|---|---|
| **All 45 materials mapped** | **PASS** | `RbxMaterialShowcaseRigEditModeTests.DumpMaterialApiEvidence_ExhaustsSupportedCatalogPlusExplicitFallback` PASS; `ProceduralCatalog_CoversEveryPublicMaterialEnumItemExactlyOnce` PASS; G11 browser `MATERIAL_CATALOG complete slots=46 mapped=45 fallback=1 failures=0 result=PASS` (36 textured, 9 procedural) in `G11_RUN_RECORD.md`. |
| **Part.Color independent, Neon is Part.Color** | **PASS** | `LuaNeonPart_PartColorReachesTheEmissionContractThroughTheBinder`, `NeonCatalogEntry_LetsPartColorOwnTheEmissionHue`, `LuaPart_MaterialReassignmentKeepsColor…` PASS; `RbxProceduralMaterialProvider.cs:33` Neon white `_MaterialColor` + `_PartColorInfluence 1`. |
| **Catalog override precedence** | **PASS** | `CatalogOverrideWinsPerMaterial` PASS; `RbxTextureMaterialProvider` merge replaces packaged entry by `Enum.Material` value (`TEXTURE_MATERIALS.md` contract). `RbxMaterialTextureCatalog.cs:MergeEntries`. |
| **Missing/incomplete texture never pink** | **PASS** | `MissingRequiredTextureFallsBackToProceduralAndLogsOnce` PASS; `PartialTextureSet_UsesProceduralFallbackThroughLuaBinderPath` PASS; `MismatchedMaterialId_ThroughBinderResolvesVisibleFallbackNotErrorShader` + `UnmappedValue_ResolvesDocumentedVisibleFallback` (magenta/black hazard) PASS. Pink shader never returned. |
| **Tile width in studs, scale tracking** | **PASS** | `TexturedTextureScale_IsCyclesPerMetre…` (4 params) PASS ×4; `TexturedTileWidth_TracksSessionScaleReplacementWithoutReallocating` PASS; `ComputeTextureScale = aspect/(tileWidth*metersPerStud)` (`PROGRESS.texqa.md:15`). |
| **AmbientCG/Bridge import correctness** | **PASS with note** | `AmbientCgMappingUsesConfirmedAssetIds` / `AmbientCgMapping_ContainsVerifiedCorrectedIds` (36 ids), `BuildDownloadUrl_UsesAmbientCgJpgZipFormat`, `MegascansScanner*` 3/3 PASS; `PROGRESS.texqa.md` verified every id via `id=` API (all 1). Packaged 6×1K CC0 + 36×2K override (476 MB, gitignored). One shelf: package `RbxMaterialTextureCatalog.asset` materialization is editor-only and not checked into source — player uses compatibility 6-set until asset exists. |

## 5. Production Lua entry points under server envelope + duplicate idempotency

| Entry | Under envelope? |
|---|---|
| `execute_lua` plain (`LuaTool.ExecuteAsync`) | **Yes** — `ExecuteLua_ProductionToolEntry_UsesServerGeneratedEnvelope` + `PlainExecuteLua_ServerEnvelope_AppliesOneMutationBatch` PASS; code `LuaTool.cs:69-90` injects `IActorIdentityProvider`, `LuaCsGameToolExecutor.cs:235-250` refuses actor mismatch. Lua-visible schema exposes only `code` (`LuaToolSchema_ExposesOnlyServerIndependentCode`). |
| MCP `execute_lua` (`ExecuteLuaMcpTool.cs:57`) | **Yes** — `Invoke_WithIdentity_UsesServerGeneratedEnvelopeOverload` PASS; previously envelope-free, now routed. |
| LoadMod chunk | **Yes** — `LoadMod_MainChunkMutation_InAclWorld_IsEnveloped` PASS |
| Scheduler resumes (`task.wait/delay`) | **Yes** — `Scheduler_ResumedTaskMutation_InAclWorld_IsEnveloped` PASS |
| Signal/remote handlers (`Heartbeat`, `RemoteEvent`) | **Yes** — `Heartbeat_HandlerMutation_InAclWorld_IsEnveloped`, `RemoteEvent_HandlerMutation_InAclWorld_IsEnveloped` PASS |
| Cross-mod export calls | **Yes** — `CrossModExportMutation_UsesCalleeActorEnvelope` PASS |
| World restore (host) | **NO** — TODO residue open (`RbxWorldPackageSerializer.cs:274`, `RbxWorldPackageContracts.cs:568`); not under envelope, fails P5. |
| **Duplicate op id applies once** | **Yes** — `ExplicitDuplicateOperationId_AppliesOnceThroughRealExecutor` + `DuplicateOperationId_ReturnsFirstProductionResult` + `MutationEnvelope_DuplicateOperationId…` PASS; ledger `DefaultMutationReplayCapacityPerActor=64` FIFO, replay-shape validation `InstanceRegistry.cs:335-347`. |

## 6. ACL in engine-free registry (not Lua-only)

**PASS** — `WorldAclAuthorizer` promoted from `LuaCsRbxInstanceBindings.cs:374-481` (internal to Mods) into `CoreAI.RbxApi.Instances` beside `InstanceRegistry`; `SetAccessControl` now refuses unauthorized caller naming actor+reason; `AuthorizeMutation_EnvelopedOwnedWrite_ByOwner_Succeeds` + registry-only destroy/set tests PASS without referencing `CoreAI.Mods` (`RungZeroAclEngineFreeTests.*` 4/4 in g9.xml). Existing Lua-path tests still pass unchanged → no regression. Host-restore gap does not undo this.

## 7. Disconnect — PlayerRemoving exactly once, no leak

**PASS (loopback)** — `RungZeroDisconnectEditModeTests.ConnectDisconnect_UsesProductionSeam_IsIdempotentAndIsolated` + `RepeatedConnectDisconnect200Actors_LeavesNoRuntimeGrowth` PASS in g9.xml; seam `ConnectActor/DisconnectActor` → `INetworkBridge.UnregisterActor` (previously dead, now wired), fires `PlayerRemoving` once, second disconnect idempotent fires nothing, releases `InGameLlmChatServiceFactory`, kills owned scheduler threads, drops `_rateWindows`/`_clientSignals`, `ActorIds` empty after 200 cycles. No ghost `Player`. Limitation: no real Mirror transport — solo/loopback only — and `RbxPlayers.RemoveActor` rollback path verified; real-network lease not measured.

## 8. Demos (15 frozen, smoke, IMGUI ratchet)

**PASS for boot/console; UI polish open** — G11 tour via `CoreAiChatExternalDriver.LoadScene` on same 960×600 player: all 15 scenes `scene-load-complete`, 0 console errors per scene (`G11_RUN_RECORD.md`). 15 in `EditorBuildSettings` frozen. IMGUI ratchet: `ImguiBanRatchetEditModeTests` allowlist 13 files (10 demo controllers + 3 diagnostics overlays), not grown; TODO confirms same 13. Notes: `ModdableUnitsDemo` mod-manager panel partly off 960px canvas, `MiniRpgModsDemo` overlay title wrong, orphan `ChatPromptButtonsController` in LiveMechanics/MiniRpg/ModdableUnits (enable menu or remove) still open.

## 9. Scale characterization — budgets frozen before measurement, zero-work = failure, no beyond-measured claim

**PASS on method, FAIL on gates — no spurious capacity claim made** — workload frozen before run: `scale.workload.json` SHA `6f5d18…`, `scale_actor.lua` `c5726c…`, `scale_server.lua` `305f80…`, recorded in every report; budgets `{4ms,16ms,1MB/min, ratio 2}` frozen. Per-frame order `ModScheduler.Advance → Heartbeat→Input→PreRender → Tick` mirrors ship. Zero-work counters (`guardedSteps, resumes, heartbeats, remoteSent/acks, partsSpawned, waitLoops, chatServed…`) cause explicit failure (`overallPass false`, scale-baseline exit 1 at ≥100). No claim beyond measured: `largestPassingActorCount 0` for full gate, doc states “Do not claim 200-player support” and `SCALE_CHARACTERIZATION.md §7` reports relaxed frame-only 100@4ms/200@16ms but full gate 0. `SCALE_CHARACTERIZATION.md §2` honestly notes host CoreCLR (not Unity Mono/IL2CPP) so numbers are lower bound, budgets must be re-confirmed on 64-bit Standalone Mono. After rung-zero re-run (rz2) slope 0.0359→0.0265 ms/actor, projected 4ms:126→154, 16ms:461→607, still full gate 0 — correctly not promoted to a capacity claim.

## 10. Documentation parity (public tools + Lua API touched today)

**PASS with one pending Hub surface** — `dev-docs/DOCS_AUDIT_2026-09-02.md` fixed every stale claim: `RbxApi.txt` + mirror `BuiltInRbxApiSkillText.cs` now document `Material`/`Orientation`/`Rotation` (YXZ/XYZ), 45-item catalog + magenta fallback + `Part.Color`, `CornerWedge` real mesh, `RemoteEvent/UnreliableRemoteEvent/RemoteFunction` creatable (Camera not creatable), `WaitForChild` yield (5s warn + timeout), `Players`/`HttpService` tree-backed, 30s RemoteFunction bound, `save_world` create-once + `load_world` confirm-only + Hub World Loads + `ConfirmedWorldMutationGate` triggers, Neon emission. `RBX_API.md`, `ROADMAP.md`, `ROBLOX_API_ROADMAP.md`, `AGENT_ROLES_AND_TOOLS.md`, `WORLD_PACKAGE.md` all updated and verified against `LuaCsRbxInstanceBindings.cs`, `ClassCatalog.cs`, etc. `CHANGELOG.md [Unreleased]` matches code: `list_autosaves`/`load_autosave` (confirm/reject pool), `load_world-pre` safety autosave, `PAYLOAD_TOO_LARGE` 65536 cap before materialization, 36-entry ambientCG catalog, proxy, etc. **One gap:** `PROGRESS.hubauto.md` autosave listing in Hub World Loads still in-progress — doc says listed, code path not yet in g9 batch.

## 11. WebGL §6.5 per-row honestly marked

**Honest** — `G11_RUN_RECORD.md` marks retry/terminal/recovery PENDING and MCP probe NOT RUN rather than claiming PASS; TODO’s “PASS on rebuilt `c38fe6fe`” claim lacks updated log/console attachment, so QA treats it as *repaired but not re-recorded*. Persistence and materials rows have real browser evidence (screenshots, `MATERIAL_CATALOG` dump, reload checks).

---

## Defects — severity ranked

| Sev | Defect | Repro |
|---|---|---|
| **BLOCKER** | **G10 throughput impossible on this backend** — 20-actor chat requires 12–25 lanes at observed provider p95 17–38s; even stub 100ms fails admission at 100 (64-slot `MaxPending` →96 refused, 400→396 at 200). Any 20-player acceptance claim would be false. | Run `artifacts/testresults` manifest §5.2 + `scale-baseline.json` N=100 `chatOffered 600 served 504 refused 96`. |
| **BLOCKER** | **Host-restore envelope missing** — `RbxWorldPackageSerializer.cs:274`, `RbxWorldPackageContracts.cs:568` write without host envelope; ACL-versioned restore not authorized through registry. Test harness would need a RED test restoring an ACL-versioned package through production composition. | `TODO.md` “Rung zero residue” OPEN; `PROGRESS.rungzero.md` §Conclusions. |
| **HIGH** | **Heap/allocation budget fails already at 20** — ≈4.5 KB/actor/frame (88KB@20→912KB@200, worst 1.87 MB/frame), heap slope 4.36 MB/min @20 vs 1 MB/min budget; GC spikes max 64ms. Scale `heapGatePass false` at 20,50,200. | `SCALE_CHARACTERIZATION.md §5` `worstHeapSlope 4.36` + `alloc/frame 88976/590472`. |
| **HIGH** | **Admission ceiling not sized to N** — `AiOrchestrationQueueOptions.cs:8 MaxPending=64` global; fix attempted and reverted because it broke `MaxPending_*` refusal tests. Chat gate fails from N=100. | Scale N=100 `chatAdmissionRefusals 96`, N=200 `396`. |
| **HIGH** | **G11 retry/terminal row not re-recorded** — first player `fdd9c7a3` left chat on typing indicator forever (`Task.Delay` on WebGL, fixed via `ILlmAsyncMarshaler.DelayAsync`); rebuilt `c38fe6fe` claimed PASS but `G11_RUN_RECORD.md` still PENDING, proxy log `artifacts/g11-proxy.log` marked PENDING. | Build log `artifacts/testresults/g11-build.log` 103 MB, run record `G11_RUN_RECORD.md:29`. |
| **MEDIUM** | **W3 follow-ups not batch-discovered** — diagnostics/autosave-load/pre-safety tests GREEN off-device (`PROGRESS.wffin.md` 6/6) but absent from `g9.xml` discovery → CI would not gate them. | `g9.xml` has no `Mvp3WorldPackageFollowUp*` tests; `PROGRESS.texqa.md:20` notes `InstanceSnapshot` mismatch blocked Mods.Tests build. |
| **MEDIUM** | **Hub autosave UI incomplete** — `PROGRESS.hubauto.md` checklist open; World Loads page does not yet list autosaves via `ListAutoSaves` / `RequestAutoLoadAsync`. | Open checklist in `PROGRESS.hubauto.md`. |
| **MEDIUM** | **Rung-zero docs still claim full P5** — `CHANGELOG` security section implies complete envelope; host-restore gap undisclosed to reader. | `CHANGELOG.md [Unreleased] Security` vs `TODO.md` residue. |
| **MEDIUM** | **Guarded VM throughput makes 4ms@20 impossible at batch 4** — 589 instr/frame total ≈29/actor before overhead (manifest §8-9); batch 256 would be needed but not yet decided/measured on Standalone Mono. Any 4ms claim now would be false. | `MVP2_ACCEPTANCE_MANIFEST.md §8-9` 148–158k instr/s. |
| **LOW** | Demo orphans: `ChatPromptButtonsController` in LiveMechanics/MiniRpg/ModdableUnits, `DemoHubPagesBinder.EnableExamplePrompts()` no-op in WaveAutoBattler | `TODO.md` Demo scene-level fixes. |
| **LOW** | IMGUI backlog 13 files on allowlist; JSON/ZIP not incrementally chunked (WebGL refuses >4MiB/4096 inst); hostile-reader canonicality not enforced | `TODO.md` backlog items. |

## Vacuous / weak tests (would stay green if the feature were broken)

| Test / pattern | Why it cannot fail | Repro |
|---|---|---|
| `Mvp1AcceptanceHarness` golden-tree + `Lint_BinderOutput_IsExactlyRobloxSpaceOutput` | Actual and expected both computed via `RbxSpace` — symmetric sign/scale bugs pass. Mitigated only by explicit chirality goldens, not by this lint. | `PROGRESS.qa-mvp1b.md` §Weak tests; `Mvp1ConversionLintEditModeTests.cs:121-153` |
| `Lookup_ByNetIdAndWorldName_ResolvesTheSameRecord` (old) | Created record then directly `BindWorldName` — passes even when lazy host-wrap absent (FALSELY CLAIMED item 12). Fixed by new `Lookup_ByWorldName_LazilyWrapsHostObject…` which actually probes host object. | `qa-mvp1b.md` Weak tests |
| `Lua_InstanceNew_DeprecatedParentArgument_WorksAndLogsOnce` | Invoked twice in one load context — passes both once-per-mod and broken once-per-load. Reloading same mod would still pass. | `qa-mvp1b.md` Weak tests |
| `AssetRule_At1To1_OnlyTheConstantChanges` (Block only) | Proved second scale only for Block; Wedge/CornerWedge broken at 1:1 would not fail it. Now extended but still per-shape. | `qa-mvp1b.md` |
| `D3_MeterAuthoredHostObjectReadsAsStuds` `1.8/0.28` | Bare arithmetic, no host object wrapped — passes without any lazy layer. | `qa-mvp1b.md` |
| Material catalog override/precedence text-match | `CatalogOverrideWinsPerMaterial` asserts override map wins — passes even if binder still resolves packaged texture because test drives `RbxMaterialTextureCatalog` directly, not the `InstanceGameObjectBinder` production path. Needs binder-level override test to be production-proven. | `RbxMaterialTextureCatalogEditModeTests.cs` |
| `CatalogShaders_NeverRaiseAnUnclampedBaseToAPower` | Static shader-source text scan — no runtime shader instantiation; a runtime `pow(negative, …)` NaN would not flip it. | `RbxMaterialCatalogQaEditModeTests.cs` |
| Archived `G10` stub measurement record (§5.1) | 40/40 served, p95 ~0.1–1.0s via scripted 100ms stub — not a provider capacity proof; doc correctly labels it “not a G10 result”, but a naive reader could cite it as PASS. | `MVP2_ACCEPTANCE_MANIFEST.md §5.1` |
| `ManyLuaParts_ReuseOneTexturedHandleWithoutNativeMaterialAllocation` | Asserts handle reuse — passes even if every Part still re-creates a material but returns equal handle name. | `RbxTextureMaterialsAcceptanceEditModeTests.cs` |

Tests already hardened this wave (no longer vacuous): `Color3_FromRGB_FractionalChannelsUseLockedCoreAiRounding` now exercises fractional channels; `CornerWedge_MaterializesCustomTwoSlopeMesh` rebuilds visual and colliders; `G6` 1-vs-19 production test; `G5` N+1 refusal with actor+reason; `G7` structural lock test.

---

## Verdict — can MVP1, MVP2, MVP2.5 be called complete?

**MVP1 — No.** The four former PARTIAL rows and the one FALSELY CLAIMED row are now code-fixed and their new focused tests pass in batch (fractional `Color3`, `CornerWedge` mesh, lazy host wrap, deprecation scoping, loud stubs), but the single harness that is supposed to prove them through production composition (`Mvp1AcceptanceHarness`) still hand-builds binder/registry/bindings (`qa-mvp1b.md:35-51`) — §3b production-path proof therefore remains incomplete for the golden-tree gate, even though ordinary mod path does use the shipped binder. Ship after harness is replaced with `CorePortableInstaller`+`CoreAiModsInstaller` composition.

**MVP2 — No.** G1–G9 and G12 now pass through the production path with negative twins and non-zero work, and rung-zero correctly moved ACL into the engine-free assembly, added server-generated envelopes to every Lua/MCP/scheduler/signal/remote entry (duplicates apply once, stale refused, forged `SenderActorId` rejected before allocation, 65 536-byte cap enforced before decode) and added an idempotent `PlayerRemoving` disconnect seam (200-cycle leak test PASS). But **G10 fails decisively on capacity** (real provider 25–57% served at 20 actors, scale admission 96/396 refused at 100/200, heap budget fails at 20), **G11 is honestly incomplete** (retry/terminal/recovery row still PENDING in the run record; MCP probe not run), and **P5 is incomplete** (host-restore writes lack an envelope). The staircase correctly makes no capacity claim, but the manifest gates remain FAILED.

**MVP2.5 persistence release — No (code substantially landed, acceptance not yet).** The runtime `.world` package (engine-free codec, stable server-authority ids, one serializer for disk + snapshot, clean mod restart, two-phase `FileRbxWorldPackageStore` durability, `ConfirmedWorldMutationGate` autosave before every `execute_lua`/`manage_mods` mutating action, Hub World Loads confirm/reject pool, WebGL `FS.syncfs` callback-only completion) and the 45-material catalog (override precedence, `Part.Color` independent, fallback visible not pink, all 46 slots `result=PASS` in Editor and G11 browser) are implemented and batch-green where discovered. The four W3 follow-ups that close the QA blockers — dangling-`PrimaryPart`→diagnostics, `list_autosaves`/`load_autosave` through confirmation, `load_world-pre` safety autosave, reserved-name rejection — are implemented and RED→GREEN off-device, but their six tests are not yet discovered in `g9.xml`, the Hub autosave UI is still todo (`PROGRESS.hubauto.md`), the WebGL browser needs a re-record after the 503-retry fix to clear the PENDING row, and the host-restore envelope residue must be closed with a RED test restoring an ACL-versioned package. Tagging `7.2.0` before those three closures would repeat the “green but not proven” error this manifest was written to prevent.

*Adversarial note:* every PASS above is cited to a batch XML entry or a browser log that can be re-parsed (`g9.xml`, `scale-baseline.json`, `G11_RUN_RECORD.md`). Every FAIL/NOT PROVEN is on the same footing — a missing log, a PENDING row, or a discovered-count mismatch — not an opinion about intent. “Green” is not evidence; the table is.

---

## Orchestrator reconciliation (2026-09-02 19:45, after the judge's pass)

The judge read `artifacts/testresults/g9.xml` and the PROGRESS files of finished tasks; the release gate is
`artifacts/testresults/g11.xml` (EditMode 3272/3263/0/9, 18:59 local) and the committed records. Each finding
re-checked against those:

| Judge finding | Status after re-check |
|---|---|
| G11 retry/terminal/recovery row PENDING | **Record defect, fixed.** `G11_RUN_RECORD_2026-09-02.md` now carries the observed timeline for both players from `artifacts/g11-proxy.log` (first player dead-wait 36 min after the injected 503; rebuilt player: 503 → attempt 2 → 200 in 5 s, block → terminal error in 46 s with controls re-enabled, unblock → new nonce without reload). MCP loopback / outbound mod HTTP probes remain NOT RUN (optional per §6.5). |
| W3 follow-up tests not batch-discovered (MEDIUM) | **Stale.** `Mvp3WorldPackageFollowUpEditModeTests` 6/6 `Passed` in `g11.xml`. |
| Hub autosave UI incomplete (MEDIUM) | **Stale.** `HubModsPages.cs` lists autosaves (`ListAutoSaves`) and routes `Load…` through `RequestAutoLoadAsync`; `CoreAiModsHubBinderFullTierEditModeTests.WorldLoadPage_Autosaves_*` 2/2 `Passed` in `g11.xml`. `PROGRESS.hubauto.md` belongs to a task that was killed and re-done. |
| CHANGELOG hides the host-restore gap (MEDIUM) | **Stale.** `Assets/CoreAI/CHANGELOG.md` Security entry ends with “Открыто: конверт для хостовых записей при восстановлении пакета мира (см. `TODO.md`)”. |
| Host-restore envelope missing (BLOCKER) | **Open, agreed.** Tracked in `TODO.md` “Rung zero residue”; restore is a host-only path behind the confirmed `load_world` gate, so it is a consistency gap of the envelope model, not a client-reachable bypass. |
| G10 throughput / admission ceiling / heap slope / guarded VM budget | **Open, agreed.** Documented in `dev-docs/SCALE_CHARACTERIZATION.md` §11 and `TODO.md`; no capacity claim is made anywhere in the docs. |
| P1 nine skipped tests | 6 `NeoxiderPagesChangedBehaviorTests` are `[Ignore]` by design (Pages module absent in this project) and 3 `RbxApi4BLiveCheck` are `[Explicit]` live-model checks; the same 9 in every run since g9. |
| P2 final round after the last fix | This judge pass ran after the last code commit `c38fe6fe`; the only later commits are records/docs. |
| Vacuous-test list | Carried into `TODO.md` backlog as-is (chirality lint, deprecation once-per-mod twin, per-shape 1:1 asset rule). |
