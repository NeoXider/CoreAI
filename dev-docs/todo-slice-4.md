# TODO slice audit — lines 890–1042 (unchecked items only)

Method: each item grepped in `Assets/`, the named files opened and read, and both
`Assets/CoreAI/CHANGELOG.md` and `Assets/CoreAiUnity/CHANGELOG.md` searched for later closures.
No closure entries were found for any item below (changelog mentions are only the F-07 audit
chain, the single-secondary fallback, and the post-hoc `JsonSchemaValidator` — none of which
close these items). Assumption: "no grep hits in `Assets/`" = not implemented, after the
targeted searches listed per row.

| TODO.md line | Item summary | Bucket | Evidence file:line | Next step |
|---|---|---|---|---|
| 890 | Hub "Audit Log" page: viewer + chain-integrity badge over `AuditLogVerifier` | OPEN | `Assets/CoreAI/Runtime/Core/Audit/AuditLogVerifier.cs:35` (verifier exists) vs `Assets/CoreAIHub/Runtime/HubBuiltInPages.cs:52-67` (only Chat/Settings/Statistics registered); `Assets/CoreAIMods/Runtime/HubIntegration/` lists only Mods/ModLogs/ModEditor/WorldState pages | Add `HubAuditLogPage` calling `AuditLogVerifier.Verify/ReadAll`, register in `HubBuiltInPages` |
| 944 | R4: UXML element factory + USS subset interpreter (`CoreAI.Source`) | OPEN | Grep `CoreAiUi\|UiCommand\|UiQuery\|UxmlFactory\|UssInterpreter\|RuntimeUi` in `Assets/CoreAI/Runtime` → no files; globs `**/*UiRuntime*`, `**/*UiSourceStore*` → no files | Implement interpreter in `CoreAI.Source` per spec; add EditMode roundtrip/parity tests |
| 951 | R4: editor materialization to `Assets/CoreAI.Generated/UI/` | OPEN | Same as above — no interpreter output exists to materialize; no `CoreAI.Generated` UI writer found | Build only after the interpreter (944) ships; add store→files writer |
| 954 | R4: theme system (`CoreAiRuntimeTheme.uss` + tokens + semantic classes) | OPEN | Glob `**/*RuntimeTheme*` → no files; grep `CoreAiRuntimeTheme` hits only `TODO.md:954` | Ship `CoreAiRuntimeTheme.uss` (+`.tss`), token-override application, class resolution + EditMode tests |
| 960 | R4: `CoreAiUiRuntime` host (UIDocument per screen, reload-callback lifecycle, router) | OPEN | Glob `**/*UiRuntime*` → no files; grep `CoreAiUiRuntime` hits only `TODO.md:960` | Create scene component + DI registration, router, idempotent rebind/`Unwire()` |
| 964 | R4: persistence via `FileUiSourceStore` + auto-restore | OPEN | Glob `**/*UiSourceStore*` → no files; grep `FileUiSourceStore` hits only `TODO.md:964` | Implement store (atomic tmp+Replace, revisions, revert, WebGL Sync) + restore on scene start |
| 970 | R4: LLM tools `ui_command` + `ui_query` | OPEN | Grep `ui_command\|ui_query\|UiCommand\|UiQuery` in `Assets/` → only `TODO.md`/`Docs/ROADMAP.md` mentions | Implement tools through standard tool policy with honest missing-element errors |
| 975 | R4: mods↔UI binding + `LuaCapabilities.Ui` tier, `ui_*` Lua API | OPEN | `Assets/CoreAIMods/Runtime/LuaExecution/LuaCapabilities.cs:11-43` (tiers end at `Full = 1<<4`, no `Ui`); grep `ui_create\|ui_set\|ui_on\|on_ui_event` → only `TODO.md:976-979` | Add `Ui = 1<<5` tier, `ui_*` bindings via execution guard, detach on unload |
| 983 | R4: animations (state-class transitions, `ui_animate` presets, tween fallback) | OPEN | No UI runtime exists to animate; grep `ui_animate` → only `TODO.md` | Ship with theme transitions + `ui_animate` presets after host (960) lands |
| 988 | R4: Hub page for runtime screens (spec source, history/revert, token editor, toggles) | OPEN | `Assets/CoreAIHub/Runtime/HubBuiltInPages.cs:52-67` (no screens page); `Assets/CoreAIMods/Runtime/HubIntegration/` has no UI-screens page | Add page reusing Lua version-store UI patterns after store (964) lands |
| 990 | R4: built-in "ui-builder" skill via skill system | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/Llm/Infrastructure/FileSkillStore.cs:26` (generic store exists, no ui-builder content); grep `ui-builder` → only `TODO.md:990` + `Docs/ROADMAP.md:229` | Author and ship the skill content (theme ref, UXML patterns, repair checklist) |
| 995 | R4: small-model acceptance gate (9B+skill / 27B builds + repairs HUD; G9/G9r) | OPEN | Grep `G9\|UiBenchmark\|builds a functional HUD` → UI benchmark exists only in `TODO.md:998-999` (`dev-docs` G9 hits are the unrelated MVP2 engine-free-ports gate) | Encode G9/G9r benchmark scenarios after tools+skill ship; run vs small-model tier |
| 1003 | R4: verification gate (EditMode + PlayMode + G9/G9r) | OPEN | Blocked by all rows above — no suites exist | Write suites as each R4 piece lands; gate the minor release on them |
| 1006 | R4: docs (`runtime-ui.md`, Lua `ui_*` docs, INSTALL, README) + minor bump | OPEN | Glob `**/runtime-ui*` → no files | Write docs as implementation lands; release all packages lockstep |
| 1014 | R5: live PlayMode compaction test (token reduction + key-fact retention) | OPEN | `Assets/CoreAiUnity/Tests/PlayMode/FastNoLlm/LlmCompactionPerRolePlayModeTests.cs:17` ("uses stub LLM only"); grep live-compaction/real-model probes → only `TODO.md:1014-1015` | Add live-model PlayMode test: long conversation → compact → assert shrink + probe facts |
| 1016 | R5: integration test that overflow-retry converges and succeeds | OPEN | `Assets/CoreAiUnity/Tests/EditMode/AiOrchestratorHistoryEditModeTests.cs:2627-2647` (stub `FailsThenOkLlm` give-up test); `Assets/CoreAiUnity/Tests/EditMode/DefaultContextBudgetPolicyEditModeTests.cs:9-32` (only the 0.75 shrink factor is unit-tested) | Add test where stub fails N>1 times then succeeds; assert prompt shrinks and call succeeds |
| 1026 | R6: wire circuit breaker into production composition root | OPEN | `Assets/CoreAI/Runtime/Core/Features/Llm/CircuitBreakerLlmClientDecorator.cs:28` (opt-in decorator exists); grep `CircuitBreaker` under `Assets/CoreAiUnity/Runtime` → no files; `Assets/CoreAiUnity/Runtime/Source/Composition/LlmPipelineInstaller.cs:215-219` wraps only `FallbackLlmClientDecorator` | Add threshold/cooldown/scope settings; wrap primary/secondary in `BuildRoutedPrimaryClientForPlatform` |
| 1028 | R6: multi-provider fallback chain (ordered list; secondary gets retry/logging wrappers) | OPEN | `Assets/CoreAiUnity/Runtime/Source/Composition/LlmPipelineInstaller.cs:215-219` (single `BuildSecondaryHttpClient`); `Assets/CoreAiUnity/Runtime/Source/Features/Llm/Infrastructure/FallbackLlmClientDecorator.cs:18-27` (exactly primary+secondary ctor) | Generalize to ordered list; wrap each leg with retry/logging decorators |
| 1030 | R6: per-provider rate limiting (token/request bucket, distinct from Lua-generation limiter) | OPEN | Grep `TokenBucket\|PerProvider\|per-provider` in `Assets/CoreAI/Runtime` → no files (only HTTP-429 retry and the chat-service sliding window limiter exist) | Add per-provider bucket decorator + settings; EditMode tests |
| 1042 | R7: provider-enforced structured output (`response_format`/`json_schema`, GBNF) | DEFERRED | Item text says "(Decide whether to build.)"; `Assets/CoreAI/Runtime/Core/Features/Crafting/JsonSchemaValidator.cs:10` is post-hoc string validation only; `ChatOptions` used in `MeaiOpenAiChatClient.cs:219,233,413,2919` with no `ResponseFormat` set (grep `ResponseFormat\|response_format` in `Assets/CoreAI/Runtime` → no files) | Owner decision first; if approved, pass `ResponseFormat`/GBNF through `MeaiOpenAiChatClient`, keep post-validation as fallback |
