# TODO slice audit — TODO.md lines 411–484 (unchecked items only)

Method per item: grepped for named symbols/files, opened and read the hits, cross-checked both changelogs.
The item starting at TODO.md:488 (guard microbenchmark) is outside the slice and excluded; the item
starting at :484 (world-command JSON) is included. No paths or line numbers below are invented — all are
from files opened during this audit.

| TODO.md line | Item summary | Bucket | Evidence file:line | Next step |
|---|---|---|---|---|
| 411 | CoreAiChatPanel god-view: extract ChatTranscriptRenderer / ChatRoutingUiController / ChatInputGate | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/Chat/CoreAiChatPanel.cs` is 4541 lines (wc); no `ChatTranscriptRenderer`/`ChatInputGate` types exist (grep hits only TODO.md); `IsChatInputAllowed` still lives inside the panel at `CoreAiChatPanel.cs:1504`; `ICoreAiRoutingUiController` seam exists at `CoreAiRoutingUiController.cs:44` but the panel still owns wiring at `CoreAiChatPanel.cs:903` | Extract transcript renderer first (bubbles/streaming/render-cap + think filter), then routing UI, then input gate; panel becomes wiring view |
| 415 | Service-locator leak: introduce CoreAiSceneScopeLocator | OPEN | No `CoreAiSceneScopeLocator` type exists (grep hits only TODO.md + prior audit doc); `FindAnyObjectByType<CoreAILifetimeScope>` still re-implemented at `CoreAiChatService.cs:60`, `TokenBudgetRuntimeSource.cs:75`, `CoreAi.cs:842`, `InGameChatPanel.cs:41`, `VisionSelfProbe.cs:216`, `AiScheduledTaskTrigger.cs:91`, `LlmUnityAutoDisableIfNoModel.cs:61`, `CoreAiBackend.cs:677` (plus `CoreAiChatPanel.cs:1762` and 2 example-game sites) | Introduce `CoreAiSceneScopeLocator`; only the facade calls it; migrate the 9+ call sites |
| 421 | Oversized adapters: MeaiLlmClient + LuaCsModRuntime extraction | OPEN | `MeaiLlmClient.cs` still 2790 lines (wc) but its two sub-tasks are DONE: transport creation lives in `LlmEndpointClientFactory.cs:40`, parsing delegates to the portable extractor at `MeaiLlmClient.cs:1915`; `LuaCsModRuntime.cs` is 3350 lines (wc — grew past the 2194 cited) and no `ModPersistenceCoordinator`/`CrossModExportBridge` types exist (grep hits only TODO.md) | Extract `ModPersistenceCoordinator` + `CrossModExportBridge` from `LuaCsModRuntime.cs`; consider closing the MeaiLlmClient half |
| 424 | Stale MoonSharp dual-VM docs | OPEN | `LuaCsModRuntime.cs:35` still says "both VMs coexist"; `LuaModsLlmTool.cs:145` still teaches "MoonSharp/Lua callback syntax"; removal confirmed shipped at `Assets/CoreAI/CHANGELOG.md:2231-2233` ("MoonSharp fully removed", 5.4.0) | Rewrite class summary + tool metadata to single-VM (Lua-CSharp) reality |
| 427 | Ambient mutable statics | OPEN | `CoreAiBackend.cs:594-595` falls back to `GameLoggerUnscopedFallback.Instance`, `:599` to `Log.Instance`, `:653` to `CoreAISettingsAsset.Instance`; `CoreAISettings.cs:16` `Instance` setter still public | Constrain so logic reads injected deps, not the statics; touch `CoreAiBackend.cs` + the four static holders |
| 431 | Copy-paste XML doc in MeaiLlmClient | OPEN | `MeaiLlmClient.cs:37-41`: the `LiveUiStreamMaxCharsPerChunk` summary contains the stray "Initializes a new instance of the current component." sentence at line 39 | Delete line 39 (one-line fix) |
| 433 | Stale generated csproj at repo root | OPEN | `ls` at repo root shows `CoreAI.RobloxApi.Binding.csproj`, `CoreAI.RobloxApi.Datatypes.csproj`, `CoreAI.RobloxApi.Instances.csproj`, `CoreAI.RobloxApi.Unity.csproj` (no matching asmdefs; current asmdefs are `CoreAI.RbxApi.*`) | Delete the four files and/or gitignore them |
| 438 | Connection registry never prunes dead connections | OPEN | `RbxScriptSignal.cs:44-52` `Disconnect()` only calls `DisconnectCore` → `_signal.Remove` (`:109-113`); `:Once` auto-disconnect path (`:79-82`) likewise never touches the registry; BUT the item overstates: `Track` prunes dead on the way in (`ModConnectionRegistry.cs:82`, `PruneDead` at `:162-172`) and the reload path drops dead entries (`:135-138`) — the leak window is dead entries lingering between `Track`/`DisconnectOwnedBy` calls | Notify the registry on `Disconnect`/`:Once`, or compact `!Connected` entries on read |
| 442 | Connection-registry threading invariant assumed, not enforced | OPEN | `ModConnectionRegistry.cs:23-24` documents "main-thread-only by invariant" with zero enforcement (no lock, no assert); `RbxScriptSignal.cs:229-232` `Remove` is unsynchronized | Assert main-thread at `Track`/`DisconnectOwnedBy` in dev builds, or lock; verify `manage_mods` marshals to main thread |
| 446 | Bare `catch { }` blocks | OPEN | Bare catches still present: `ToolExecutionPolicy.cs:330` (swallows `CanonicalizeArguments` failure), `ToolExecutionPolicy.cs:888` (returns empty list), `AiOrchestrator.cs:1159` (`/* swallow */` on enumerator dispose), `AiOrchestrator.cs:2060` (plain-text tool-result parse, WHY-commented), `LuaCsExecutionGuard.cs:232-235` (`EndGuard` re-arm). Note: the cited line numbers drifted — `AiOrchestrator.cs:1057` and `ToolExecutionPolicy.cs:309` are typed catches today | Distinguish cancelled predecessors from faulted ones; make outer cancel short-circuit serialized calls |
| 449 | CoreAISettings lock asymmetry | OPEN | `CoreAISettings.cs:8-11` WHY comment explicitly admits readers racing `ResetOverrides` can observe a partially cleared set; `ResetOverrides` locks (`:448`) while override backing fields are read/written without it (`:441-445` doc: "only two concurrent resets are serialized") | Decide per the item: fix (snapshot under lock) or accept explicitly and check the box |
| 452 | IMGUI overlay in shipped demo scenes | OPEN | `MiniRpgModsDemo.unity:929` and `LiveMechanicsModsChatDemo.unity:744` still embed `CoreAiTokenBudgetOverlay`; UITK replacement exists (`CoreAiTokenBudgetUiView.cs:12`, `TokenBudgetHubPage.cs:9` "replaces the floating IMGUI overlay") | Swap the two demo scenes to the UITK view (or drop the overlay), save scenes |
| 455 | Test quality leftovers | OPEN | `.Result` on main thread still at `GameConfigPlayModeTests.cs:62,71,97,119` (each inside `[UnityTest]`); `PlayModeTestAwait.cs:12-15` 3-arg overload passes null CTS and `:39-42` documents the resulting leak ("could not be cancelled ... leaks into the following tests"); 4-arg path cancels + `ObserveFaultsOfAbandonedTask` (`:66-73`) mitigates; machine-mangled (mojibake/Cyrillic) comments visible at `GameConfigPlayModeTests.cs:55,84-86,105` | Await via `PlayModeTestAwait` in the four tests; fix/cancel the 3-arg path; restore comment text (~18 files per item — scope unverified here) |
| 459 | Hub duplication/dead code | OPEN | `HubSettingsPage.cs:1872` vs `:1915` are near-identical GGUF dropdown builders (differ only in label string and target field); `SetPlaceholder` at `:1959-1981` is an event-driven hint overlay with NO timer — the item's "200 ms polling timer" claim does not match current code (no timer found in the method) | Unify the two dropdown builders; re-verify the polling-timer claim before acting on it |
| 465 | Boxing guard seam on the hot path | OPEN | `IScriptExecutionGuard.cs:11-18` exposes only boxing `Invoke` overloads (no void overload); `LuaCsModRuntime.cs:1926` is `params object[]`; `:1903-1905` `handlers.ToArray()` per event; `:1918` allocates `new object[2]` via params; `RbxScriptSignal.cs:242` (`FireCore`) also `ToArray()`s — i.e. the "scratch-buffer pattern" to reuse does not exist yet either | Add non-boxing void overload + reusable `LuaValue[]` scratch + non-params 2-arg overload; add scratch snapshot to signal dispatch |
| 471 | DynamicInvoke per call on polled binding surface | OPEN | `LuaCsApiRegistry.cs:225` `callback.DynamicInvoke(args)` on the shared `unity_*`/`input_*` dispatch path (`CreateFunction` at `:217-226`, args coerced per call at `:224`) | Replace with direct-delegate/compiled dispatch (typed `Func<>`/`Action<>` fast path) |
| 473 | Per-property-read dispatch probes | OPEN | Item's cited lines (149-207) are stale — that region is now authority checks + `WrapInstance`; the real read path is `Instance.__index` at `LuaCsRbxInstanceBindings.cs:439-529`: switch, then `TryReadNetworkMember`, method lookup + `IsA` ancestry (`:472-476`), `SignalBehavior`+`IsA("Workspace")` (`:478`), camera/userinput/runservice/clickdetector/modelpivot/spatial probes (`:483-511`), `FindFirstChild` (`:520`); `WrapInstance` (`:172-187`) caches proxies but no class-kind bitmask | Compute class-kind bitmask once at wrap time; short-circuit the probe chain |
| 476 | Double allocation per datatype crossing | OPEN | `LuaCsRbxValues.cs:140-143` `Box` takes an already-boxed `object` and wraps it in `LuaCsRbxValueBox` (non-generic holder at `:16-24`); no generic `LuaCsRbxValueBox<T>` exists (grep confirms); identity documented non-semantic via `__eq` at `:10-15` | Add generic `LuaCsRbxValueBox<T>` holding the struct directly |
| 479 | Adaptive hook batch (2.65× speedup, needs design) | DEFERRED | Fixed `HookInstructionBatch = 4` at `LuaCsExecutionGuard.cs:96`; of the listed open items, "`EndGuard` must restore the enclosing window" is DONE (`:217-235` re-arms the enclosing hook), and step charging exists (`:316-318`); still missing: allocation-headroom-sized re-arm, batch-aware budget charging, allocation-bomb test at several heap sizes; host-side bench exists (`tools/vmbench/Program.cs`) but no checked-in EditMode benchmark; blocked on design + `TODO(guard-tight-loop-latency)` | Write the design (budget charging by batch in force), then implement adaptive re-arm + heap-size-parameterized bomb test |
| 484 | World-command JSON alloc per command | OPEN | `LuaCsWorldRuntimeBindings.cs:591` `JsonUtility.ToJson(env, false)` + envelope + command per `Publish` (`:584-599`); no checked-in guidance found steering per-frame animation to the zero-alloc part sink (`IPartPropertySink` exists at `LuaCsRbxInstanceBindings.cs:165` but docs wording not verified in this pass) | Add the guardrail doc (per-frame animation → part sink, not `coreai_world_change`); optionally pool/reuse the envelope |
