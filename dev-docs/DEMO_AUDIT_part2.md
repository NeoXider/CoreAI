# DEMO AUDIT part2 — 5 scenes vs the BRIGHT/SELLING/MINI-TUTORIAL bar

Method: grepped each `.unity` for `m_Script: {fileID: 11500000, guid: …}` and GameObject/Camera/Light/UI blocks; resolved every guid with `grep -rl "<guid>" --include=*.meta` over `Assets`, `Packages`, `Library/PackageCache`; grepped every referenced `.cs` for `OnGUI`/`GUILayout`. Zero files edited.

| Scene | Camera+Light | Instruction text | What it sells | IMGUI | Broken? |
|---|---|---|---|---|---|
| `Assets/CoreAI.Demos/LuaMods/LuaModsDemo.unity` | Camera "Main Camera" (solid bg default blue-gray). Light "Directional Light", white, intensity 1 — single default light, flat. | NONE — no Canvas, no TMP, no UIDocument (0 RectTransforms). | Lua modding: a wave-director world-edit mod plus a damage-formula logic-override slot (`LuaModsDemoController` + `WaveDirectorMod.lua.txt` + `DamageTunerMod.lua.txt` + `RbxWorldHost`). | HARD FAIL — `Assets/CoreAI.Demos/LuaMods/Scripts/LuaModsDemoController.cs:98` `private void OnGUI()`, `GUILayout.*` lines 100–202. | No missing scripts (`m_Script: {fileID: 0}` count 0, all guids resolve). "Boss"/"Ground" present. Only interaction is the banned IMGUI panel. |
| `Assets/CoreAI.Demos/MiniRpg/MiniRpgModsDemo.unity` | Camera "First Person Camera" (+ Neo first-person controller, step audio). Light "Directional Light", white 1. Flat. | NONE per strict bar — a UIDocument exists but `sourceAsset: {fileID: 0}` (no UXML assigned, renders nothing). Nearest copy is chat prompt buttons: title `"Mini-RPG mod tasks (easy → hard)"`, labels `'Easy: torch flicker (light)'`, `'Medium: day/night cycle'`, `'Hard: village quest (inter-mod)'`. | Live Lua modding of a mini-RPG through chat: torch flicker, day/night cycle, co-operating spawner/quest mods (`ChatPromptButtonsController` + `CoreAiHubDemo/Window` + `RbxWorldHost`). | HARD FAIL ×2 — `Assets/CoreAI.Demos/LiveMechanicsMods/Scripts/LiveMechanicsModsChatPersistenceController.cs:329` `OnGUI` (+`GUILayout` to line 571); `Assets/CoreAiUnity/Runtime/Source/Features/Diagnostics/CoreAiTokenBudgetOverlay.cs:183` `OnGUI`. | UIDocument with unassigned sourceAsset (dead UI slot). No missing scripts; all guids resolve (incl. Neo `PlayerController3DPhysics`, URP `Lit.mat`). |
| `Assets/CoreAI.Demos/ModdableUnits/ModdableUnitsDemo.unity` | Camera "Main Camera" (solid default bg). Light "Directional Light", white 1. Flat. | Prompt-button labels only, no dedicated overlay — UIDocument on "CoreAiChatUI" hosts `CoreAiChat.uxml` (36 lines, generic chat chrome, zero demo copy) + `CoreAiChatPanel` + EventSystem. Title `"Unit Forge mod prompts"`, labels `"Start a battle"`, `"Add archers and boss"`, `"Endless waves"`. | Mod-forged units: chat-written mods call `forge_define` to add archers/ogre boss and endless waves (`ModdableUnitsDemoController`). | HARD FAIL ×2 — `Assets/CoreAI.Demos/ModdableUnits/Scripts/ModdableUnitsDemoController.cs:349` `OnGUI` (+`GUILayout` to line 404); `LiveMechanicsModsChatPersistenceController.cs:329` as above. | 2 of 3 prompt `Prompt:` fields EMPTY (`"Start a battle"`, `"Endless waves"` — dead buttons). No missing scripts; all guids resolve. |
| `Assets/CoreAI.Demos/MultiplayerFoundation/MultiplayerFoundationDemo.unity` | Camera "Main Camera" (Skybox clear, dark navy bg). Lights "Key Light" directional 1.25 cool-white + "Cyan Accent Light" point, cyan, intensity 5, range 12. Best basic rig of the five, still one-note. | NONE — no Canvas/TMP/UIDocument/RectTransform anywhere. | Actor-scoped multiplayer mods over a shared Rbx world (`MultiplayerFoundationDemoController`, `actorCount: 4`, + `RbxWorldHost` + `CoreAiModsHubBinder`). | PASS — none in `MultiplayerFoundationDemoController.cs`, `CoreAiModsHubBinder.cs`, scopes, `RbxWorldHost`, `CoreAiLuaWorldModule`. | No missing scripts; all guids resolve. No on-screen interactive component at all (no EventSystem, no UI, no buttons). |
| `Assets/CoreAI.Demos/ProceduralMaterials/ProceduralMaterialsShowcase.unity` | Cameras (5): "View 1 - Mid Far Sweep", "View 2 - Face On Close", "View 3 - Grazing Close", "View 4 - Glass Ice Backdrop", "View 5 - Neon HDR Bloom". Lights "Large Grazing Key Light" directional 1.65 with shadows + "Neutral Low Fill" point 2.2 + bloom Volume. Richest presentation. 859 GameObjects, zero RectTransforms/Canvas, no EventSystem (keyboard-driven). | World-space legacy `TextMesh` 3D labels ONLY — no screen-space overlay. Control copy quoted: `'Q/E OR ARROWS: MATERIAL  \|  1-5: VIEW  \|  SPACE: SWEEP Label'` and `'VIEW 1/5: MID/FAR SHIMMER SWEEP MATERIAL: Metal \| Q/E SELECT \| 1-5 VIEW \| SPACE SWEEP RUNNING'`. | Runtime procedural Rbx materials: ~20+ pedestal samples on a judging rig with 5 staged views (`RbxProceduralMaterialsShowcase` + `ConstantRotator`). | PASS — none in `RbxProceduralMaterialsShowcase.cs` or `ConstantRotator.cs`. | No missing scripts; all guids resolve (Volume, URP camera data). Uses legacy `TextMesh` instead of TextMeshPro; no screen-space overlay. |

## Verdict — all 5 FAIL the strict bar

- **LuaModsDemo — FAIL.** Missing: (1) remove IMGUI (`LuaModsDemoController.cs:98`), (2) add screen-space instruction overlay (Canvas/TMP or UIDocument) telling the user what to press, (3) art-directed lighting beyond one default directional.
- **MiniRpgModsDemo — FAIL.** Missing: (1) remove IMGUI (`LiveMechanicsModsChatPersistenceController.cs:329`, `CoreAiTokenBudgetOverlay.cs:183`), (2) assign the UIDocument's `sourceAsset` or delete the dead slot and add a real instruction overlay (prompt-button copy alone is not an overlay), (3) lighting pass.
- **ModdableUnitsDemo — FAIL.** Missing: (1) remove IMGUI (`ModdableUnitsDemoController.cs:349`, persistence controller as above), (2) a real instruction overlay, (3) fill the two empty `Prompt:` fields (`"Start a battle"`, `"Endless waves"`), (4) lighting pass.
- **MultiplayerFoundationDemo — FAIL (IMGUI-clean).** Missing: (1) any instruction text at all, (2) any on-screen interactive component (no UI/EventSystem — nothing to press).
- **ProceduralMaterialsShowcase — FAIL, closest (IMGUI-clean, sells hardest).** Missing: (1) a screen-space instruction overlay (Canvas/TMP or UIDocument) — controls currently exist only as world-space legacy `TextMesh`; (2) swap `TextMesh` labels to TextMeshPro.

Confirmation: all 5 named scenes audited; the only file created is `dev-docs/DEMO_AUDIT_part2.md` (plus `PROGRESS.demo-part2.md`); no file edited, no commit.
