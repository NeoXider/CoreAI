# TODO slice audit — lines 1054–1134 (unchecked items only)

Method: for each item, grepped for named symbols, opened the files below, and cross-checked both changelogs. Line numbers are from files actually read in this session.

| TODO.md line | Item summary | Bucket | Evidence file:line | Next step |
|---|---|---|---|---|
| 1054 | Per-profile fallback + limits (decorator/timeout/retry are global-only) | OPEN | `Assets/CoreAiUnity/Runtime/Source/Composition/LlmPipelineInstaller.cs:215-218` (fallback wraps only the global `settings.HasValidFallbackBackend` client); `Assets/CoreAiUnity/Runtime/Source/Api/CoreAiBackend.cs:601-603` (hot-swap rebuilds only the legacy fallback) | Decide per-profile story: per-profile secondary + timeout/retry in `LlmRoutingManifest`/`LlmClientRegistry`; touch `LlmPipelineInstaller.cs`, `LlmClientRegistry.cs` |
| 1056 | Hot-swap consistency (`SetApiKey/SetApiBaseUrl` skip profile clients) | OPEN | `Assets/CoreAiUnity/Runtime/Source/Api/CoreAiBackend.cs:578-606` (`TryHotSwap` calls only `registry.SetLegacyFallback`, never `ApplyManifest`/`ApplyRouteTable`); documented caveat `Assets/CoreAiUnity/Docs/RUNTIME_BACKEND_SWITCHING.md:240` | Re-`ApplyManifest`/`ApplyRouteTable` on key/URL change in `CoreAiBackend.TryHotSwap`; regression test in `CoreAiBackendEditModeTests.cs` |
| 1060 | Docs: "per-agent providers" recipe (N × `OpenAiHttpLlmSettings` + manifest, zero code) | OPEN | `Assets/CoreAI/Docs/LLM_ROUTING.md:80-139` read end-to-end: routing contracts + body params, no per-agent-providers recipe; phrase search hits only `TODO.md:1060` itself | Write the recipe doc (inspector-only path) + link from `LLM_ROUTING.md`; no code change |
| 1065 | PlayMode `[Explicit]` round-trip vs real vision model | OPEN | `Assets/CoreAiUnity/Tests/PlayMode/FastNoLlm/CameraLlmToolPlayModeTests.cs:9` (FastNoLlm capture test exists, as the item says); `[Explicit]`+vision search finds no live capture→model→assert test | Add `[Explicit]` PlayMode test: `CameraLlmTool` capture → vision model → assert, with model-override pattern from `AiAttachmentLivePlayModeTests.cs:188` |
| 1072 | `SubAgentDefinition` | OPEN | Symbol search: only `TODO.md:1072` + design doc `TODO/MultiAgent_Orchestration_v2.0.md:58-64` (spec, no implementation); no `SubAgentDefinition.cs` exists | Implement per design doc §1 (`Assets/CoreAI/Runtime/Core/Features/Orchestration/SubAgentDefinition.cs`, NEW) |
| 1073 | `IAgentRegistry` + `ExecuteSubAgentAsync` + `ExecuteSubAgentsParallelAsync` | OPEN | Same search: only `TODO.md:1073` + design doc §3–§4 (`TODO/MultiAgent_Orchestration_v2.0.md:121-191`); no registry/orchestrator sub-agent code in `Assets/CoreAI/Runtime/Core/Features/Orchestration/` | Implement registry + `AgentOrchestrator.ExecuteSubAgentAsync` with context isolation, then bounded parallel variant |
| 1074 | `AgentLlmTool` (parent-only) + DI + per-role exposure | OPEN | Same search: only `TODO.md:1074` + design doc `TODO/MultiAgent_Orchestration_v2.0.md:92-134`; no `AgentLlmTool` class in codebase | Implement MEAI tool + `AgentMemoryPolicy.GetToolsForRole` wiring + settings |
| 1075 | R9 EditMode tests + docs + CHANGELOG | OPEN | Follows from the three rows above (nothing to test/document yet); no R9 entries in either CHANGELOG | After 1072–1074: EditMode isolation/parallel tests, docs, changelog entry |
| 1079 | Audit log retention (rotated files never deleted; corrupt-tail append loop) | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/Audit/AuditLogWriter.cs:459-472` (rotation, no deletion policy); `:239-241` (corrupt tail → `ChainReset` marker, keeps writing same file) | Add retention policy (cap rotated-file count/age) + corrupt-tail quarantine (roll to fresh file); touch `AuditLogWriter.cs` + tests |
| 1082 | Lost-update races (Revert vs MutateAsync; Save/Delete; overlay store; rolling summary) | OPEN | `Assets/CoreAI/Runtime/Core/Features/AgentMemory/IAgentMemoryStore.cs:163-178` (MutateAsync gates) vs `:229-256` (`Revert` does raw TryLoad/Save, no gate); `Assets/CoreAiUnity/Runtime/Source/Features/Llm/Infrastructure/FileSkillStore.cs:81-93,246-263` (Save/Delete take only `_gate`, bypass path-keyed `MutationLocks` at `:111`); `Assets/CoreAiUnity/Runtime/Source/Features/Lua/Infrastructure/FileDataOverlayVersionStore.cs:24` (`_ioLock` only, no cross-instance lock / reload-on-change) | Unify on the path-keyed gates; add cross-instance lock + reload to overlay store; make rolling-summary RMW atomic |
| 1086 | Test flake: `Dispose_CompletesPendingTask` gets `TaskCanceledException` | OPEN | `Assets/CoreAI/Runtime/Core/Features/Orchestration/QueuedAiOrchestrator.cs:1382-1389` (Dispose drains pending with `ObjectDisposedException`); in-flight task racing Dispose can surface `TaskCanceledException` instead — structure matches the reported flake; could not execute the suite here | Fix dispose race (order drained-pending completion vs active-task cancellation) and harden the test to accept-or-retry both exceptions |
| 1089 | Small cheap items (collisions; empty `tool_names` wipe; Save/Reset ghosts; zombie reps; autosave 0) | OPEN | `Assets/CoreAI/Runtime/Core/Features/Llm/ManageSkillsLlmTool.cs:117-123` + `:174-180` (empty string parses to empty list → allowlist wiped; only `null` preserves — confirms the bug); exception: `WorldStateAutoSaveHook` 0-case looks DONE via `Assets/CoreAIMods/Runtime/HubIntegration/WorldStateAutoSaveHook.cs:37-42` (0 is forwarded to disable). Collisions/ghosts/zombie not checked — assumed open | Guard empty-`tool_names` in `ManageSkillsLlmTool.Update`; then handle collisions, ghost snapshot, zombie reps as separate one-liners |
| 1096 | `SmartToolCallingChatClient` per-request shared-instance state | OPEN | `Assets/CoreAI/Runtime/Core/Features/Llm/SmartToolCallingChatClient.cs:66-76` (unsynchronized instance props) reset per request at `:107`, `:140`, `:190`, `:343`, `:399` — concurrent `GetResponseAsync` calls interleave, exactly as described | Return per-call context alongside the response when DI sharing / concurrency arrives; touch `SmartToolCallingChatClient.cs` + `MeaiLlmClient.cs:152` call site |
| 1102 | `ChainReset` accounting limits (unkeyed chain, forged reset) | STALE | Premise outdated: `Assets/CoreAI/Runtime/Core/Audit/AuditLogVerifier.cs:123-143` now fails closed on mid-file `ChainReset` (returns `Ok=false`), and `:154-158` already supports keyed `AuditHash.HmacChain(hmacKey, …)`; chained-set path delegates to the same check (`:188-193`) | Reword or drop the item; remaining work (if any) is wiring the HMAC key through, not inventing it |
| 1106 | Summarization-off hard truncation coherence | DEFERRED | `Assets/CoreAI/Runtime/Core/Features/AgentMemory/DeterministicConversationContextManager.cs:51-63` (no-partition path returns raw-budget history) + `:131-140` (plain cap limiter); item itself frames it as a note with no wire violation | Add `ShouldPartition`-style hysteresis only if incoherent mid-exchange splits are observed in practice |
| 1110 | Pending-parent ownership in additive scenes (last-wins static owner) | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/World/WorldStateManager.cs:510-511` (`_pendingParentOwner = this` on every load) + `:542-549` (static `ForgetPendingParent` routes to last owner) — matches the item; partial fix (claim-on-success) shipped per `Assets/CoreAiUnity/CHANGELOG.md:1137` | Per-manager routing (executor resolves its scene's manager via DI); broader additive topology (`FindObjectsByType` in Save/DestroyAll) is the unblocker |
| 1116 | Persisted summary exceeds token cap by fold-marker line | OPEN | `Assets/CoreAI/Runtime/Core/Features/AgentMemory/DeterministicConversationContextManager.cs:100-104` (limiter runs BEFORE `Stamp`, comment admits marker can never be trimmed) + `Assets/CoreAI/Runtime/Core/Features/AgentMemory/ConversationHistoryPartition.cs:395-405` (`Stamp` appends the marker line after limiting) | Reserve marker headroom inside the limiter (`ConversationRolledSummaryLimiter` / `LimitSummaryIfNeeded`) |
| 1120 | `AgentMemoryPolicy.ConfigureChatHistory` missing null/whitespace guard | OPEN | `Assets/CoreAI/Runtime/Core/Features/AgentMemory/AgentMemoryPolicy.cs:292-309` (`roleId?.Trim()` then `TryGetValue(null)` → throws) vs sibling `:314-319` which has the `IsNullOrWhiteSpace` early-return | Copy the guard from `ConfigureLlmContextCompaction`; add null/whitespace/empty regression test |
| 1129 | `DelegateLlmTool` boundary IL2CPP verification | OPEN | `Assets/CoreAI/Runtime/Core/Features/Llm/DelegateLlmTool.cs:89-95` (still classifies via `AsyncStateMachineAttribute` reflection) + `:110-135` (stack-frame-dependent sync/async fault split); no built-player verification found in either CHANGELOG | Verify in a built IL2CPP player per RUNTIME-first rule; fallback is a construction-time async-shape flag instead of stack inspection |
| 1134 | `TryLoad` skipped-load edge (`_unresolvedObjects` kept while links wiped) | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/World/WorldStateManager.cs:390-435` (all early returns clear `_pendingParentByChildId` at `:397` but leave `_unresolvedObjects` intact — only `:480` on the success path replaces it); `Save()` re-appends them at `:324-339` | Clear/align `_unresolvedObjects` on the skipped-load returns, or skip the re-append when the pending map was wiped |

Notes: items starting after line 1134 (benchmark green-on-empty, timeout chunk mutation, router theft at 1137–1143) are outside the assigned slice and were not judged. No UNVERIFIABLE items — every verdict above rests on a file actually opened and read.
