# Dev notes moved out of the user documentation

Internal notes that used to sit inside user-facing documents. They were moved here on 2026-09-17 so the
user docs describe only the product; each entry names the document it came from.

## From `Docs/ARCHITECTURE_RULES.md` §6 (an HTML comment under the `WHY:` rule)

TODO: a one-time cleanup pass over the mods/RbxApi code that strips over-applied or redundant `WHY:`
comments and keeps only the genuinely non-obvious ones. It was planned to run together with the
Roblox->Rbx identifier rename (see BACKLOG); that rename has landed (`RbxSpace` and friends), so the
cleanup pass is the part still open. No owner was recorded.
