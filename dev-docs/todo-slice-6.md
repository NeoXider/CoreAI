# TODO slice audit — TODO.md lines 1137–1221 (unchecked items only)

Method: for each item, grepped for named symbols, opened the files, checked behaviour. Cross-checked `Assets/CoreAI/CHANGELOG.md` and `Assets/CoreAiUnity/CHANGELOG.md`.

| TODO.md line | Item summary | Bucket | Evidence file:line | Next step |
|---|---|---|---|---|
| 1137 | Benchmark user-stop with zero results passes green; consider `Assert.Inconclusive` | OPEN | `Assets/CoreAIBenchmark/Tests/PlayMode/Benchmarks/GameCreationBenchmarkPlayModeTests.cs:442-449` — stop with zero results only logs a warning + `yield break` (green), no `Assert.Inconclusive` | Add `Assert.Inconclusive` (or explicit stop-aborted verdict) in that branch; touch only that file + changelog |
| 1139 | Timeout decorator streaming rewrite mutates inner chunk in place (latent for caching third-party clients) | OPEN | `Assets/CoreAI/Runtime/Core/Features/Llm/TimeoutLlmClientDecorator.cs:303-310` — rewrites `current.ErrorCode`/`current.Error` on the inner client's instance, no clone | Clone the terminal chunk before correcting code/text; add a test with a caching stub `ILlmClient` |
| 1142 | Router cross-generation clear theft (stale router nulls `CommandReceived` 1→0) | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/Messaging/Infrastructure/AiGameCommandRouter.cs:99-116` — refcount only, no generation token; `ResetStatics` at `:24-28` nulls unconditionally. CHANGELOG `:1163` fixed underflow only, not this | Add a generation token alongside `_activeRouterCount`; touch router + `AiGameCommandRouterMainThreadPlayModeTests.cs` |
| 1146 | Benchmark window/menu G8 divergence before any run writes `PrefG8` | OPEN | `Assets/CoreAiUnity/Tests/EditMode/GameCreationBenchmarkWindowUitk.cs:72` (in-memory `_runG8=true` default) vs `Assets/CoreAiUnity/Tests/EditMode/GameCreationBenchmarkLauncher.cs:140-160` (`LoadSavedG8Preference` migration); G8 migration fixes in CoreAiUnity CHANGELOG `:1140-1159` did not remove the pre-prefs window default | Initialise window toggles from `LoadSavedG8Preference()` before first render (already done in `LoadPrefs` `:1278` — verify menu path reads the same source); cosmetic |
| 1149 | `LastRoundtripPromptTokens` only set by MEAI client when `Usage != null` | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/Llm/Infrastructure/MeaiLlmClient.cs:263-278` — field stamped only inside `if (response.Usage != null)`; no other producer sets it | Add the field to any new usage-reporting client's terminal path + a stub-client regression test |
| 1153 | Multi-scope audit writers share one `audit.jsonl` with independent `_seq`/`_prevHash` | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/Audit/AuditLogWriter.cs:25-27` (per-instance `_seq`/`_prevHash`), `:55-57` (default ctor targets the same shared folder); no singleton/shared-writer | Design decision: per-scope audit files vs process-wide shared writer singleton; then implement + test interleaved writes |
| 1157 | Extractor skips backtick/quote-cited spans; lax local models' real tool JSON stalls | DEFERRED | `Assets/CoreAI/Runtime/Core/Features/Orchestration/LlmToolCallTextExtractor.cs:95-100` — citation skip still in place, no trailing-lone exception; deliberate tradeoff per item text | Unblocked by G-benchmark evidence of stalls on LLMUnity models; then add trailing-lone-cited-block exception |
| 1162 | Verify `File.Replace` on WebGL; add smoke check or `File.Move` fallback | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/Audit/AuditLogWriter.cs:296` (`File.Replace`, no platform fallback; only `CoreAiWebGlPersistence.Sync()` at `:297`); same pattern at `Assets/CoreAI/Runtime/Core/Features/AgentMemory/FileConversationSummaryStore.cs:311` | Add WebGL smoke check or `File.Move` fallback on that platform; Roadmap `Docs/ROADMAP.md:256` still lists it |
| 1165 | Mods threading: shared `ILuaTransactionScope` + `LuaCsExecutionGuard` hook `Stack` thread invariant | OPEN | `Assets/CoreAIMods/Runtime/Infrastructure/LuaCsModRuntimeFactory.cs:333` (bindings are the shared scope of both surfaces); `LuaCsGameToolExecutor.cs:388,412` (`ResetTransactions()` in `finally`); `Assets/CoreAIMods/Runtime/Scripting/LuaCs/LuaCsExecutionGuard.cs:90` (`ConditionalWeakTable<LuaState, Stack<GuardHook>>`, no thread-id assert found) | Per-surface scope (or assert/document single-thread contract) + debug thread-id assert in guard entry |
| 1173 | Remove dead `GetExclusiveEndForSafeUnboundRawStreaming` | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/Llm/Infrastructure/MeaiLlmClient.cs:2470` — method still present; only references are its own tests (`Tests/EditMode/MeaiLlmClientEditModeTests.cs:1405-1424`) and docs (`Docs/STREAMING_ARCHITECTURE.md:268`) | Confirm no production caller (grep shows none), then delete method + tests + doc reference |
| 1174 | Separate inter-token idle timeout distinct from total request timeout in SSE | DONE | `Assets/CoreAI/Runtime/Core/Features/Llm/TimeoutLlmClientDecorator.cs:16-20,296-299` (streaming path is an idle/no-progress budget, re-armed per chunk) + `Assets/CoreAI/Runtime/Core/Features/Llm/MeaiOpenAiChatClient.cs:835,906-945` (`streamIdleTimeoutSeconds`, `ReadWithIdleTimeoutAsync`); Core CHANGELOG `:1210-1212` records end-to-end idle enforcement | None — verified present |
| 1175 | Surface `reasoning_content` SSE deltas as collapsible "thinking" channel | DEFERRED | `Assets/CoreAI/Runtime/Core/Features/Orchestration/ILlmClient.cs:262-268` (docs: hosts "can render a collapsible thinking section") + `MeaiLlmClient.cs:904-910` (dedicated `ReasoningText` chunk field "so the UI can render a collapsible thinking…"); deliberately kept internal per item note, no chat-panel UI renders it | Unblocked by a UI decision to actually surface the channel in `CoreAiChatPanel`; plumbing already exists |
| 1180 | Tests: per-tool timeout firing; Lua bomb + blocking-native-binding; EditMode CI gate | OPEN | Per-tool timeout covered (`Tests/EditMode/ResilienceFeaturesEditModeTests.cs:159-223`); allocation bomb covered (`LuaCsSecureSandboxEditModeTests.cs:87`); but no blocking-native-binding test found and CI gate only partially (module gates in `CONTRIBUTING.md:26`) | Add blocking-native-binding test; confirm/extend CI EditMode gate, then re-check this item |
| 1182 | Chat: queue outgoing messages while a turn is in progress | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/Chat/CoreAiChatPanel.cs:2414-2424` (`TrySendInput` returns when busy — stop or drop) and `:2486-2491` (external submit "ignored (chat busy)"); no queue/flush | Add pending-message queue in `CoreAiChatPanel`, flush in order on turn completion; cover with EditMode test |
| 1184 | Move `unity_find`/`unity_set_position` mutation assertion into PlayMode suite | OPEN | No PlayMode test uses `unity_find`/`unity_set_position` (grep over `Tests/PlayMode` and `CoreAIMods/Tests` finds none); `FullAccessDemoScenePlayModeTests.cs:26-66` only asserts scene loads with empty world. CHANGELOG `:2635` claims a Full-tier PlayMode test but it is not the mutation assertion | Add `unity_find` + `unity_set_position` live-scene mutation test to the PlayMode suite |
| 1191 | `RecordingWorldExecutor.InvalidCommandCount` split from `ToolCalls` — document or fold | DONE | `Assets/CoreAI/Runtime/Core/Features/Benchmarking/BenchmarkReportFormatter.cs:124,127` — report surfaces "invalid world commands" as its own `Invalid` column distinct from "Tool calls"; the split is explicitly documented in the artifact | None — split explicitly surfaced in the report |
| 1195 | Benchmark's manually-built orchestrator turn-trace visible in Agent Session Inspector | OPEN | No `AgentTurnTrace`/trace-sink reference under `Assets/CoreAIBenchmark` (grep: none); inspector only resolves a trace reader from scene DI (`Editor/Diagnostics/AgentSessionInspectorWindow.cs:495-544`) | Record the harness's manual trace into an `InMemoryAgentTurnTraceSink` (or expose an `IAgentTurnTraceReader`) the inspector can resolve |
| 1221 | `RoleFitness` Orchestrator/Director rates small models 9+/10 off single-turn G1-G8 | DEFERRED | `Assets/CoreAI/Runtime/Core/Features/Benchmarking/RoleFitness.cs:90-96` — honest caveat `Note` present, formula/weights/gates untouched, exactly as the item describes; Core CHANGELOG `:2609`, CoreAiUnity `:1937` record the caveat | Unblocked by a user decision (changing weights affects historical comparisons) + a genuinely multi-turn scenario feeding the Director gate |
