# TODO slice audit — unchecked items, TODO.md lines 488–886

Method per item: grepped the named symbols, opened the files below, checked behaviour in code,
cross-checked `Assets/CoreAI/CHANGELOG.md` and `Assets/CoreAiUnity/CHANGELOG.md` for later closures
(a changelog grep for `copyable|generation lease|disposed.*resolv|idempotency.*executor|Audit Log.*page|Mods tab.*WebGL`
returned zero hits — none of this slice was closed by a later release without unchecking).
`dev-docs/TODO_AUDIT_2026-09-04.md` exists from a prior run; it was not used as evidence — every
citation below is a file this run opened and read. Paths are repo-relative; line numbers are exact.

| TODO.md line | Item summary | Bucket | Evidence file:line | Next step |
|---|---|---|---|---|
| 488 | Guard-overhead microbenchmark (LUA_PERF §6), fold into F-20 | OPEN | `Assets/CoreAIMods/Runtime/Scripting/LuaCs/LuaCsExecutionGuard.cs:326-330` — the "~6% (measured)" sampling claim lives inline in code; grep for `no-hook`, `batch-64`, `GuardOverhead` in `Assets/` finds no EditMode benchmark test | Add the no-hook / batch-4 / batch-64 fixed-loop EditMode benchmark; file under the F-20 suite |
| 642 | Mod system UX (MVP5): `Mods/<ModName>/` + `mod.json`, script-context folders, enable/disable, hot reload, C# API | OPEN | `Assets/CoreAIMods/Runtime/Infrastructure/FileLuaModSourceStore.cs:25-26` — layout is `manifest.json` + single `main.lua`, not `mod.json` + `server/`/`client/`/`shared/`; enable/disable exists (`FileLuaModSourceStore.cs:194-227` `SetActive`), hot reload exists (`ReloadMod` across `LuaCsModRuntime.cs`), C# API exists (`ILuaModRuntime.cs:30`) | Implement folder mods: `mod.json` (with `api_version` per `Docs/CoreAIMods/ROBLOX_API_ROADMAP.md:595-596`) + script-context→folder mapping; keep `SetActive`/`ReloadMod` semantics |
| 666 | No generation lease between resolve and execution (`InFlightRequests` only inside `CompleteAsync`) | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/Llm/Infrastructure/LlmClientRegistry.cs:191-204` — `TrackedEndpointClient` does `Interlocked.Increment` only inside `CompleteAsync`; `ResolveClientForRole` (`LlmClientRegistry.cs:369-412`) hands out a bare client with no lease | Add resolve→execute lease (e.g. generation token pinned at resolve, honoured by the release path); touches `LlmClientRegistry.cs` + `ILlmClient` |
| 670 | Disposed registry keeps resolving (no `_disposed` guard) | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/Llm/Infrastructure/LlmClientRegistry.cs:232-259` sets `_disposed` in `Dispose`, but `ResolveClientForRole` (`LlmClientRegistry.cs:369-412`) never checks it | Add `ObjectDisposedException` guards under `_gate` in `ResolveClientForRole` / `ResolveContextWindowForRole` / `ResolveExecutionModeForRole` |
| 673 | Offline + persisted role→profile surfaces `RoutingUnavailableClient` instead of falling through | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/Llm/Infrastructure/LlmClientRegistry.cs:393-396` returns `RoutingUnavailableClient` for an inactive-routed profile; `:1503-1510` deliberately keeps restored endpoints `Inactive` in Offline mode with no fallthrough to `_legacyFallback` | When `UseOffline`, fall an inactive-routed role through to the offline/legacy client as the item suggests; regression test with persisted `_runtimeRoleProfiles` + Offline settings |
| 736 | `TODO(guard-tight-loop-latency)`: body-less `while true do end` cut only after ~8 s | OPEN | `Assets/CoreAIMods/Runtime/Scripting/LuaCs/LuaCsExecutionGuard.cs:92-96` + `:205-214` — enforcement is purely the instruction-count hook (batch 4); full file (lines 20-361) contains no watchdog thread and no finer hook granularity; default wall-clock net is 10 s (`LuaCsExecutionGuard.cs:119-121`) | Add a wall-clock watchdog thread or finer hook granularity for body-less loops; bombs with bodies already trip promptly via `:330-334` |
| 744 | Allocation guard is a per-call first-growth backstop (documented Mono limitation) | DEFERRED | `Assets/CoreAIMods/Runtime/Scripting/LuaCs/LuaCsExecutionGuard.cs:341-348` WHY comment — states the per-call/first-growth semantics explicitly and classifies trips by type | Unblocked by: a per-thread allocation counter on Unity Mono (does not exist — `GC.GetAllocatedBytesForCurrentThread` returns 0, `:306-311`), or a per-mod allocation-rate watchdog design; bounded sustained allocation stays covered by step/time budgets |
| 750 | LLM-API PlayMode gate needs a live OpenAI-compatible endpoint (spark/opencode) | OPEN | `Assets/CoreAiUnity/Tests/PlayMode/LlmVerification/AgentMemoryOpenAiApiPlayModeTests.cs:32-37` — `TestAgentSetup` + `Assert.Ignore` when no backend is ready: the gate is still conditional, no stood-up local server in repo | Stand up the local OpenAI-compatible server and run these + Lua-authoring pipeline + model-behavior audit |
| 795 | Manual interaction drivers for every demo + screenshots (startup smoke ≠ UX acceptance) | OPEN | `dev-docs/G11_RUN_RECORD_2026-09-02.md:33` — 15-demo tour is explicitly "**PASS for boot/console**; UI-polish notes recorded" (scene-load + zero console errors only) | Write per-demo interaction drivers (buttons, input, battle loops, F9/F10, mod persistence) and capture screenshots; `Assets/Screenshots/` is git-ignored (`.gitignore:109-111`), so decide where acceptance shots live |
| 802 | `TODO(moddableunits-binding-seam)`: wire `UnitForgeLuaBindings` into the mod runtime | OPEN | `Assets/CoreAI.Demos/ModdableUnits/Scripts/ModdableUnitsDemoController.cs:93-100` — seam exists, `_bindings` is constructed but "What remains … is composition plumbing"; README still aspirational | Thread `AdditionalGameplayBindings` through `CoreAiModsInstaller.RegisterCoreAiMods` + `CoreAiModsLifetimeScope` with lazy `IUnitForge` lookup, PlayMode-validate, restore README claim |
| 809 | Demo hygiene: untrack `AutoSaves/`, MiniRpg local-LLM wiring, build-settings scenes | OPEN | `.gitignore:106-107` ignores `AutoSaves/` but `git ls-files Assets/Scenes/AutoSaves` still lists 14 tracked files (rule added after committing); `MiniRpgModsDemo.unity` no longer contains `13333` (grep count 0 — that sub-part is done); `ProjectSettings/EditorBuildSettings.asset:8-52` already lists 15 scenes (that sub-part is done) | `git rm --cached Assets/Scenes/AutoSaves/` (+ the `InitTestScene*` roots if also stale); remaining: `ResolveModsContainer` throw and `ChatPromptButtonsController` IL2CPP reflection notes |
| 826 | Verify AI writes mods via Hub chat per Hub-enabled demo (4B/9B/27B + Opus) | OPEN | `dev-docs/G11_RUN_RECORD_2026-09-02.md:33-34` — covers boot tour plus scripted proxy tool-call probes only; no per-demo Hub-chat mod-authoring runs recorded anywhere in `dev-docs/` | Run Hub-chat mod-writing per kept Hub demo on LM Studio 4B/9B/27B and Opus via `agent.sh openai-server` bridge; record results |
| 838 | F-18: pin floating Git dependencies + upgrade command | OPEN | `Packages/manifest.json:3-9` — 7 bare git URLs with no tag/commit (only `jp.hadashikick.vcontainer` at `:27` pins `#1.17.0`) | Pin every git URL to tag/commit and add an explicit upgrade command |
| 840 | F-19: slim dev project (Epic Toon FX ~522 MiB) or minimal verification project | OPEN | `Assets/Epic Toon FX/` directory read lists full Demo/Materials/Models/Prefabs/Scripts/Sound/Textures tree (528 MiB on disk); no `Samples~` relocation exists under `Assets/` | Move demo assets to `Samples~` or split a minimal verification project; re-measure checkout size |
| 842 | F-20: performance regression suite (enqueue, streaming, 10k queries, revisions, audit burst, WebGL cadence) | OPEN | `Assets/CoreAIBenchmark/Tests/PlayMode/Benchmarks/BenchmarkSuiteCatalog.cs:15-27` — the only suite is G1-G8 game-creation benchmarks, none of the six listed regression areas | Create the six-area regression suite (guard microbenchmark from :488 folds in here) |
| 847 | F-21: replace fixed `Task.Delay` waits in async tests with signal-based waits | OPEN | `Assets/CoreAiUnity/Tests/EditMode/QueuedAiOrchestratorEditModeTests.cs:567` — `await Task.WhenAny(studentB, Task.Delay(2000))`; repo-wide grep still finds dozens of `Task.Delay` waits in `CoreAiUnity/Tests` | Convert remaining fixed waits to signal-based (`TaskCompletionSource`/event-driven) waits, starting with the orchestrator/streaming suites |
| 851 | Cross-request idempotency: executor-level stable idempotency keys | OPEN | `Assets/CoreAI/Runtime/Core/Features/Llm/ToolExecutionPolicy.cs:143-156` — doc comment says "Call at the start of each top-level request" and `Reset()` clears signatures/traces: replay state is request-local by design | Add executor-level stable keys surviving `Reset()`; touches `ToolExecutionPolicy.cs` + orchestrator request plumbing |
| 863 | Hub log text should be copyable | OPEN | `Assets/CoreAIMods/Runtime/HubIntegration/HubModLogsPage.cs:249-275` — `BuildLine` renders every log line as plain non-selectable `Label`s; no read-only `TextField`, no selection support anywhere in the file | Render lines in selectable read-only `TextField`s (or enable UITK text selection) in `HubModLogsPage.cs` (+ diagnostics viewer if shared) |
| 866 | WebGL build: Hub has no Mods tab | UNVERIFIABLE | Searched: `CoreAiModsHubBinder.cs:43-57` (no platform gating — only null-window/null-scope early-outs), `ProjectSettings.asset:853` (WebGL defines include `COREAI_HAS_HUB;COREAI_LLM;COREAI_LUA`, ruling out the item's hypothesized cause), `HubModsPages.cs:50-94` (unconditional registration), changelog grep for a fix (zero hits). Inconclusive because the symptom lives in a WebGL player whose scene composition cannot be inspected from editor code | Next: check the WebGL Hub scene hierarchy for `CoreAiModsHubBinder` + `CoreAiModsLifetimeScope`, then reproduce in a WebGL build and inspect which early-out (or strip) fires |
| 886 | Durability: WebGL sync after `Reset`; two-phase audit rotation; surface audit failures at runtime | OPEN | `Assets/CoreAiUnity/Runtime/Source/Features/World/WorldStateManager.cs:587-592` — Reset→`PersistFsForWebGl()` in `finally` is DONE (`CoreAiUnity/CHANGELOG.md:274-277` + `WorldStateManagerResetPersistenceEditModeTests.cs:11`); `Assets/CoreAiUnity/Runtime/Source/Features/Audit/AuditLogWriter.cs:256-300` — recoverable two-phase rotation (marker/anchor + partial-rotation reuse) is DONE | Remaining: surface audit worker failures during runtime (currently only log + requeue/retry paths), not just at Dispose/testing flush |
| 891 | Hub "Audit Log" page: viewer + chain-integrity badge over `AuditLogVerifier` | OPEN | `Assets/CoreAI/Runtime/Core/Audit/AuditLogVerifier.cs:35-36` — verifier exists with `ReadAll`/`Verify`; `HubModsPages.cs:85-93` + `:97-112` register only Mods/Logs/WorldLoads pages; grep for `HubAudit|AuditPage` finds nothing | Add the viewer page + integrity badge over `AuditLogVerifier`, register alongside the Mods tab |
