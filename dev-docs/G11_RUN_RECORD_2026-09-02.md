# G11 WebGL browser-run record — 2026-09-02

Protocol: `dev-docs/MVP2_MULTIPLAYER_PLAN.md` §6.5 (G11 verdict is decided from a served
`artifacts/G11-WebGL` player built by `CoreAI.Editor.CoreAIG11WebGlBuild.Build`, never from Play Mode).
Every row below is filled from the actual run; `PENDING` means not yet observed.

## Run identity

| Field | Value |
|---|---|
| Build commit | First player: `5c62c43d` (build attempt 2). Rebuilt player after the retry defect: `c38fe6fe` (build attempt 4) — the verdict below is decided on the rebuilt player. Both with working-tree `CoreAISettings.asset` pointed at the proxy (`apiBaseUrl: http://127.0.0.1:8811/v1`, `modelName: ling-3.0-tiny`) |
| Build log | `artifacts/testresults/g11-build.log` (overwritten by the last attempt) — attempt 2: `G11 WebGL build succeeded: 103090094 bytes in 00:15:28`; attempt 4: `G11 WebGL build succeeded: 103127272 bytes in 00:16:18` |
| Build start / end | Attempt 2: 18:07:48 → 18:23 local (the first attempt failed on a WebGL-only compile error fixed in `fdd9c7a3`), 103,090,094 bytes, 15 min 28 s. Attempt 4: 19:05 → 19:21 local (attempt 3 failed because `http.server` still held the output folder), 103,127,272 bytes, 16 min 18 s |
| Served URL / origin | `http://127.0.0.1:8777/` (python `http.server`, Brotli with decompression fallback) |
| Browser | Claude Code in-app browser, `Chrome/148.0.7778.280` (WebGL 2.0 / OpenGL ES 3.0 Chromium) |
| OS | Windows 11 Pro 10.0.26200 |
| Provider endpoint in the player | `http://127.0.0.1:8811/v1` → G11 proxy → LM Studio `http://127.0.0.1:1234/v1` |
| Model | `ling-3.0-tiny` (LM Studio, `lms ps` at build time) |
| Outer request timeout | 120 s (`requestTimeoutSeconds`), budget 125 s |
| Console / Network trace | `artifacts/g11-proxy.log` (every forwarded/injected request with UTC timestamps; the 14:2xZ block is the rebuilt player). The browser console was read in-session through the page log hook (`window.__coreaiLogs`, installed by the served `index.html` patch) and is quoted in the rows below; it was not exported to a file |

## §6.5 rows

| Row | Required observation | Result |
|---|---|---|
| Player boot and continued frames | FullAccessDemo canvas, hub/chat accept input, animation continues ≥10 s | PASS (boot 18:3x; hub tabs switch on canvas input; Tetris keeps animating, see below). Console at boot: 3 URP `Hidden/*` shader-unsupported messages (CoreCopy, StencilDitherMaskSeed, HDRDebugView — WebGL2 limitation, rendering intact), `[CoreAI.LocalModel]` limitation warning, no exceptions; the 8 LlamaLib errors of the previous run are gone. In-game debug console after self-test + Tetris: **0 errors, 1 warning, 15 info** |
| Lua scheduling and world mutation | `Run self-test` → `PASS` with `SELFTEST_DONE`, checks>0, `fails=0`; Tetris pieces fall and respond | Self-test: `[LuaPlatformExample] SELFTEST PASS: SELFTEST_DONE checks=16 fails=0` (mods platform_selftest_a/b loaded and unloaded); Tetris: PASS — `Start Tetris` loads mod `tetris3d`, the Lua-authored well and a falling piece appear (`first part materialized: tz1_wl1 ... shader=CoreAI/Rbx/Procedural Surface`), the piece keeps falling across screenshots 4 s apart and moves on `Nudge left`; no player freeze while coroutines yield |
| Real-provider chat under latency | nonce request visible in Network, answer bubble contains the nonce, canvas responsive while pending | **PASS** — prompt "Reply with exactly this token and nothing else: NONCE-7d3f" via `SubmitPrompt`; page log `MeaiOpenAiChatClient: POST (stream) http://127.0.0.1:8811/v1/chat/completions (attempt 1/10)` → `stream HTTP 200 ... reading SSE body`; proxy log shows the forwarded request; answer bubble `NONCE-7d3f`, `LLM < (stream) wallMs=17004 chunks=7`, `turn-complete (10 chars): NONCE-7d3f`. While pending the hub switched to the Lua Platform tab and the Tetris piece kept moving between screenshots |
| Retry, terminal failure, recovery | one injected 503 retried → nonce; persistent block → terminal error within 125 s, controls re-enabled; restore → new nonce succeeds without reload | **FAIL on the first player `fdd9c7a3`, PASS on the rebuilt player `c38fe6fe`.** First player: `/control/fail-next` armed (proxy 13:46:19Z), the prompt hit the injected 503 (13:46:39Z) and the chat stayed on the typing indicator with no second request for 36 min — the retry backoff was a `Task.Delay`, which never completes on the single-threaded browser runtime (fixed in `MeaiOpenAiChatClient` by routing every backoff and the stream idle timeout through `ILlmAsyncMarshaler.DelayAsync`, regression `MeaiOpenAiChatClientWebGlDelayEditModeTests`). Rebuilt player: fail-next armed 14:24:12Z → prompt → 503 at 14:25:09Z → page log `attempt 2/10` → 200 stream at 14:25:14Z (5 s after the 503) → answer bubble with the new nonce; `/control/block` at 14:25:57Z → prompt at 14:26:19Z → eleven 503s with growing backoff (14:26:19Z … 14:26:58Z) → terminal error bubble in the chat 46 s after the prompt, send controls re-enabled, no page reload; block lifted 14:27:43Z → new prompt at 14:28:01Z → 200 stream at 14:28:04Z → new nonce answered in the same page session. All three sub-rows observed in the browser and in `artifacts/g11-proxy.log` |
| WebGL persistence | `Save Now` → `Has saved state: Yes` → reload → `Yes`; `Reset World` → `No` → reload → `No`; no syncfs error | Step 1: page loaded with `Has saved state: Yes` (state from an earlier session survived in IDBFS); `Reset World` → `No`, log `[WorldState] Reset: all world objects destroyed, save file deleted.`; reload (same origin, `?coreai-external-driver=1`) → `Has saved state: No` ✓; the persisted `tetris3d` mod rehydrated from the IDBFS mod store and kept playing after the reload; `Save Now` → `Has saved state: Yes` with the page line "Browser storage flush confirmed — this state survives a reload" (log `[WorldState] Saved 0 objects.` — the page state, not object count, is what the flag tracks); reload → `Has saved state: Yes` ✓, log `[WorldState] Save file found, auto-loading... Loaded 0 objects (0 failed)`. No syncfs warning or error in the captured console. **PASS** |
| Fail-closed behaviour | MCP loopback unavailable, local models excluded, mod HTTP refused promptly | Local models: `[CoreAI.LocalModel] Local models are not available in the browser...` at boot and in both Qwen demos (documented limitation, no dead wait). MCP loopback and outbound mod HTTP were **not probed** in this run (no in-player entry point without a chat turn; optional per §6.5) — recorded as NOT RUN, not as PASS. |
| Materials | 45 `MATERIAL slot=NN/46` records + `MATERIAL_CATALOG complete slots=46 mapped=45 fallback=1 failures=0 result=PASS`; no pink shader; seams judged on close-ups | Scene loaded through the external driver (`scene-load-complete: ProceduralMaterialsShowcase`); `DumpMaterialApiEvidence` in the browser: 46 `MATERIAL slot` records, **36 on `CoreAI/Rbx/Textured Surface`** (the 2K override catalog shipped in this build), 9 procedural, 1 diagnostic fallback, `MATERIAL_CATALOG complete slots=46 mapped=45 fallback=1 failures=0 result=PASS`, 0 console errors. Face-on close-ups in the browser (rig view 2, `SelectMaterial` via `SendMessage`): Brick, Cobblestone, DiamondPlate, Slate, Marble, Grass, CeramicTiles, RoofShingles render their 2K sets with visible normal relief on cube, bevelled cube, sphere, cylinder and plane — no seams, no pink error shader; Metal reads darker than in the Editor because the WebGL rig reflects the dark studio (metalness map + no realtime probe), not a shader failure; FALLBACK shows the intended magenta/black hazard. The one in-game "error" during this pass was the harness itself (`MissingMethodException: SetSweepEnabled` — `SendMessage` cannot pass a bool), not the player. **PASS** |
| 15 demos | each scene boots, UI usable wide/narrow, console clean | Toured via `CoreAiChatExternalDriver.LoadScene` on the same player (960×600 canvas): FullAccessDemo ✓, ProceduralMaterialsShowcase ✓, CoreAiHubDemo ✓ (Hub chat, tabs), LiveMechanicsDemo ✓ (battle log ticking), LiveMechanicsModsChatDemo ✓ (mod manager + token budget overlays), WaveAutoBattlerModsDemo ✓, LuaModsDemo ✓, MiniRpgModsDemo ✓, ModdableUnitsDemo ✓ (mod-manager panel partly off the 960 px canvas — layout note), MultiplayerFoundationDemo ✓ (4 actors, 10/10 isolation checks PASS on screen); QwenGenieDemo ✓ and QwenSpellcraftDemo ✓ (both show the documented "Local models are not available in the browser" line instead of a dead wait), SkillsDemo ✓, WorldCommandsDemo ✓, CoreAiChatDemo ✓ (UITK chat). All 15: `scene-load-complete` logged, 0 console errors/exceptions per scene. Notes for the demo backlog: LiveMechanics*/LuaMods/Skills/WorldCommands/Qwen* still draw IMGUI panels (tracked by the IMGUI ratchet); the ModdableUnits mod-manager panel is partly outside the 960 px canvas; the MiniRpg mod-manager overlay is titled "Wave Auto-Battler Mod Manager". **PASS for boot/console; UI-polish notes recorded** |

## Window sizes

Fixed 960×600 canvas from the default Unity WebGL template: at 480×800 the template scales the whole
player down and every hub/chat control stays readable; at 768×1024 the default template crops the
canvas on the left. UI Toolkit panels scale with the canvas (no overlap, no clipped text inside the
canvas). Recommendation for release: a responsive template (`unity-desktop` container width 100%).

## Verdict

**G11 PASS on the rebuilt player `c38fe6fe` (build attempt 4)** for every §6.5 row that was run:
boot, Lua scheduling/world mutation, real-provider chat, retry/terminal/recovery, WebGL persistence,
materials, 15 demos. The first player (`fdd9c7a3`) FAILED the retry row and the defect was fixed in
production code, not in the test. Not run: MCP loopback and outbound mod HTTP probes. Known benign
console lines: three URP `Hidden/*` shader-unsupported messages on WebGL2.
