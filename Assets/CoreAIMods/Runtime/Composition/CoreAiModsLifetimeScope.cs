using VContainer;
using VContainer.Unity;

namespace CoreAI
{
    public class CoreAiModsLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}
