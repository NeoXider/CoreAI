{
  "name": "com.nexoider.coreaiunity",
  "version": "1.5.7",
  "displayName": "CoreAI Unity",
  "unity": "6000.0",
  "description": "Unity layer for CoreAI: MEAI, CoreAISettingsAsset, CoreAiChatPanel, HTTP retry, context compaction, etc. v1.5.7: WebGL — CoreAiChatService marshals to main thread after RunTaskAsync (orchestrator uses ConfigureAwait(false)); CoreAi.AskAsync no longer uses ConfigureAwait(false); SendStreamingAsync switches per chunk + finally on WebGL; RunAgentTurnAsync catch/error paths + explicit PlayerLoopTiming.Update. Prior WebGL chat fixes 1.5.4–1.5.6. Depends on com.nexoider.coreai 1.5.5+.",
  "dependencies": {
    "com.nexoider.coreai": "1.5.5"
  }
}
