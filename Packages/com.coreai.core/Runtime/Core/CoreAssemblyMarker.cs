namespace CoreAI
{
    /// <summary>
    /// Маркер сборки <see cref="CoreAI.Core"/> (портативное ядро без UnityEngine).
    /// </summary>
    public static class CoreAssemblyMarker
    {
        /// <summary>Имя сборки для тестов и диагностики (совпадает с CoreAI.Core.asmdef).</summary>
        public const string AssemblyName = "CoreAI.Core";
    }
}
